(globalThis.TURBOPACK || (globalThis.TURBOPACK = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/@swc/helpers/cjs/_interop_require_default.cjs [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
exports._ = _interop_require_default;
}),
"[project]/node_modules/@swc/helpers/cjs/_interop_require_wildcard.cjs [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) return obj;
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") return {
        default: obj
    };
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) return cache.get(obj);
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) Object.defineProperty(newObj, key, desc);
            else newObj[key] = obj[key];
        }
    }
    newObj.default = obj;
    if (cache) cache.set(obj, newObj);
    return newObj;
}
exports._ = _interop_require_wildcard;
}),
"[project]/node_modules/react/cjs/react.development.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * @license React
 * react.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
"use strict";
"production" !== ("TURBOPACK compile-time value", "development") && function() {
    function defineDeprecationWarning(methodName, info) {
        Object.defineProperty(Component.prototype, methodName, {
            get: function() {
                console.warn("%s(...) is deprecated in plain JavaScript React classes. %s", info[0], info[1]);
            }
        });
    }
    function getIteratorFn(maybeIterable) {
        if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
        maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
        return "function" === typeof maybeIterable ? maybeIterable : null;
    }
    function warnNoop(publicInstance, callerName) {
        publicInstance = (publicInstance = publicInstance.constructor) && (publicInstance.displayName || publicInstance.name) || "ReactClass";
        var warningKey = publicInstance + "." + callerName;
        didWarnStateUpdateForUnmountedComponent[warningKey] || (console.error("Can't call %s on a component that is not yet mounted. This is a no-op, but it might indicate a bug in your application. Instead, assign to `this.state` directly or define a `state = {};` class property with the desired state in the %s component.", callerName, publicInstance), didWarnStateUpdateForUnmountedComponent[warningKey] = !0);
    }
    function Component(props, context, updater) {
        this.props = props;
        this.context = context;
        this.refs = emptyObject;
        this.updater = updater || ReactNoopUpdateQueue;
    }
    function ComponentDummy() {}
    function PureComponent(props, context, updater) {
        this.props = props;
        this.context = context;
        this.refs = emptyObject;
        this.updater = updater || ReactNoopUpdateQueue;
    }
    function noop() {}
    function testStringCoercion(value) {
        return "" + value;
    }
    function checkKeyStringCoercion(value) {
        try {
            testStringCoercion(value);
            var JSCompiler_inline_result = !1;
        } catch (e) {
            JSCompiler_inline_result = !0;
        }
        if (JSCompiler_inline_result) {
            JSCompiler_inline_result = console;
            var JSCompiler_temp_const = JSCompiler_inline_result.error;
            var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            JSCompiler_temp_const.call(JSCompiler_inline_result, "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.", JSCompiler_inline_result$jscomp$0);
            return testStringCoercion(value);
        }
    }
    function getComponentNameFromType(type) {
        if (null == type) return null;
        if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
        if ("string" === typeof type) return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return "Fragment";
            case REACT_PROFILER_TYPE:
                return "Profiler";
            case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
            case REACT_SUSPENSE_TYPE:
                return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            case REACT_ACTIVITY_TYPE:
                return "Activity";
        }
        if ("object" === typeof type) switch("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof){
            case REACT_PORTAL_TYPE:
                return "Portal";
            case REACT_CONTEXT_TYPE:
                return type.displayName || "Context";
            case REACT_CONSUMER_TYPE:
                return (type._context.displayName || "Context") + ".Consumer";
            case REACT_FORWARD_REF_TYPE:
                var innerType = type.render;
                type = type.displayName;
                type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
                return type;
            case REACT_MEMO_TYPE:
                return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
            case REACT_LAZY_TYPE:
                innerType = type._payload;
                type = type._init;
                try {
                    return getComponentNameFromType(type(innerType));
                } catch (x) {}
        }
        return null;
    }
    function getTaskName(type) {
        if (type === REACT_FRAGMENT_TYPE) return "<>";
        if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE) return "<...>";
        try {
            var name = getComponentNameFromType(type);
            return name ? "<" + name + ">" : "<...>";
        } catch (x) {
            return "<...>";
        }
    }
    function getOwner() {
        var dispatcher = ReactSharedInternals.A;
        return null === dispatcher ? null : dispatcher.getOwner();
    }
    function UnknownOwner() {
        return Error("react-stack-top-frame");
    }
    function hasValidKey(config) {
        if (hasOwnProperty.call(config, "key")) {
            var getter = Object.getOwnPropertyDescriptor(config, "key").get;
            if (getter && getter.isReactWarning) return !1;
        }
        return void 0 !== config.key;
    }
    function defineKeyPropWarningGetter(props, displayName) {
        function warnAboutAccessingKey() {
            specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, console.error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)", displayName));
        }
        warnAboutAccessingKey.isReactWarning = !0;
        Object.defineProperty(props, "key", {
            get: warnAboutAccessingKey,
            configurable: !0
        });
    }
    function elementRefGetterWithDeprecationWarning() {
        var componentName = getComponentNameFromType(this.type);
        didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = !0, console.error("Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."));
        componentName = this.props.ref;
        return void 0 !== componentName ? componentName : null;
    }
    function ReactElement(type, key, props, owner, debugStack, debugTask) {
        var refProp = props.ref;
        type = {
            $$typeof: REACT_ELEMENT_TYPE,
            type: type,
            key: key,
            props: props,
            _owner: owner
        };
        null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
            enumerable: !1,
            get: elementRefGetterWithDeprecationWarning
        }) : Object.defineProperty(type, "ref", {
            enumerable: !1,
            value: null
        });
        type._store = {};
        Object.defineProperty(type._store, "validated", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: 0
        });
        Object.defineProperty(type, "_debugInfo", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: null
        });
        Object.defineProperty(type, "_debugStack", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugStack
        });
        Object.defineProperty(type, "_debugTask", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugTask
        });
        Object.freeze && (Object.freeze(type.props), Object.freeze(type));
        return type;
    }
    function cloneAndReplaceKey(oldElement, newKey) {
        newKey = ReactElement(oldElement.type, newKey, oldElement.props, oldElement._owner, oldElement._debugStack, oldElement._debugTask);
        oldElement._store && (newKey._store.validated = oldElement._store.validated);
        return newKey;
    }
    function validateChildKeys(node) {
        isValidElement(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
    }
    function isValidElement(object) {
        return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    function escape(key) {
        var escaperLookup = {
            "=": "=0",
            ":": "=2"
        };
        return "$" + key.replace(/[=:]/g, function(match) {
            return escaperLookup[match];
        });
    }
    function getElementKey(element, index) {
        return "object" === typeof element && null !== element && null != element.key ? (checkKeyStringCoercion(element.key), escape("" + element.key)) : index.toString(36);
    }
    function resolveThenable(thenable) {
        switch(thenable.status){
            case "fulfilled":
                return thenable.value;
            case "rejected":
                throw thenable.reason;
            default:
                switch("string" === typeof thenable.status ? thenable.then(noop, noop) : (thenable.status = "pending", thenable.then(function(fulfilledValue) {
                    "pending" === thenable.status && (thenable.status = "fulfilled", thenable.value = fulfilledValue);
                }, function(error) {
                    "pending" === thenable.status && (thenable.status = "rejected", thenable.reason = error);
                })), thenable.status){
                    case "fulfilled":
                        return thenable.value;
                    case "rejected":
                        throw thenable.reason;
                }
        }
        throw thenable;
    }
    function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
        var type = typeof children;
        if ("undefined" === type || "boolean" === type) children = null;
        var invokeCallback = !1;
        if (null === children) invokeCallback = !0;
        else switch(type){
            case "bigint":
            case "string":
            case "number":
                invokeCallback = !0;
                break;
            case "object":
                switch(children.$$typeof){
                    case REACT_ELEMENT_TYPE:
                    case REACT_PORTAL_TYPE:
                        invokeCallback = !0;
                        break;
                    case REACT_LAZY_TYPE:
                        return invokeCallback = children._init, mapIntoArray(invokeCallback(children._payload), array, escapedPrefix, nameSoFar, callback);
                }
        }
        if (invokeCallback) {
            invokeCallback = children;
            callback = callback(invokeCallback);
            var childKey = "" === nameSoFar ? "." + getElementKey(invokeCallback, 0) : nameSoFar;
            isArrayImpl(callback) ? (escapedPrefix = "", null != childKey && (escapedPrefix = childKey.replace(userProvidedKeyEscapeRegex, "$&/") + "/"), mapIntoArray(callback, array, escapedPrefix, "", function(c) {
                return c;
            })) : null != callback && (isValidElement(callback) && (null != callback.key && (invokeCallback && invokeCallback.key === callback.key || checkKeyStringCoercion(callback.key)), escapedPrefix = cloneAndReplaceKey(callback, escapedPrefix + (null == callback.key || invokeCallback && invokeCallback.key === callback.key ? "" : ("" + callback.key).replace(userProvidedKeyEscapeRegex, "$&/") + "/") + childKey), "" !== nameSoFar && null != invokeCallback && isValidElement(invokeCallback) && null == invokeCallback.key && invokeCallback._store && !invokeCallback._store.validated && (escapedPrefix._store.validated = 2), callback = escapedPrefix), array.push(callback));
            return 1;
        }
        invokeCallback = 0;
        childKey = "" === nameSoFar ? "." : nameSoFar + ":";
        if (isArrayImpl(children)) for(var i = 0; i < children.length; i++)nameSoFar = children[i], type = childKey + getElementKey(nameSoFar, i), invokeCallback += mapIntoArray(nameSoFar, array, escapedPrefix, type, callback);
        else if (i = getIteratorFn(children), "function" === typeof i) for(i === children.entries && (didWarnAboutMaps || console.warn("Using Maps as children is not supported. Use an array of keyed ReactElements instead."), didWarnAboutMaps = !0), children = i.call(children), i = 0; !(nameSoFar = children.next()).done;)nameSoFar = nameSoFar.value, type = childKey + getElementKey(nameSoFar, i++), invokeCallback += mapIntoArray(nameSoFar, array, escapedPrefix, type, callback);
        else if ("object" === type) {
            if ("function" === typeof children.then) return mapIntoArray(resolveThenable(children), array, escapedPrefix, nameSoFar, callback);
            array = String(children);
            throw Error("Objects are not valid as a React child (found: " + ("[object Object]" === array ? "object with keys {" + Object.keys(children).join(", ") + "}" : array) + "). If you meant to render a collection of children, use an array instead.");
        }
        return invokeCallback;
    }
    function mapChildren(children, func, context) {
        if (null == children) return children;
        var result = [], count = 0;
        mapIntoArray(children, result, "", "", function(child) {
            return func.call(context, child, count++);
        });
        return result;
    }
    function lazyInitializer(payload) {
        if (-1 === payload._status) {
            var ioInfo = payload._ioInfo;
            null != ioInfo && (ioInfo.start = ioInfo.end = performance.now());
            ioInfo = payload._result;
            var thenable = ioInfo();
            thenable.then(function(moduleObject) {
                if (0 === payload._status || -1 === payload._status) {
                    payload._status = 1;
                    payload._result = moduleObject;
                    var _ioInfo = payload._ioInfo;
                    null != _ioInfo && (_ioInfo.end = performance.now());
                    void 0 === thenable.status && (thenable.status = "fulfilled", thenable.value = moduleObject);
                }
            }, function(error) {
                if (0 === payload._status || -1 === payload._status) {
                    payload._status = 2;
                    payload._result = error;
                    var _ioInfo2 = payload._ioInfo;
                    null != _ioInfo2 && (_ioInfo2.end = performance.now());
                    void 0 === thenable.status && (thenable.status = "rejected", thenable.reason = error);
                }
            });
            ioInfo = payload._ioInfo;
            if (null != ioInfo) {
                ioInfo.value = thenable;
                var displayName = thenable.displayName;
                "string" === typeof displayName && (ioInfo.name = displayName);
            }
            -1 === payload._status && (payload._status = 0, payload._result = thenable);
        }
        if (1 === payload._status) return ioInfo = payload._result, void 0 === ioInfo && console.error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))\n\nDid you accidentally put curly braces around the import?", ioInfo), "default" in ioInfo || console.error("lazy: Expected the result of a dynamic import() call. Instead received: %s\n\nYour code should look like: \n  const MyComponent = lazy(() => import('./MyComponent'))", ioInfo), ioInfo.default;
        throw payload._result;
    }
    function resolveDispatcher() {
        var dispatcher = ReactSharedInternals.H;
        null === dispatcher && console.error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.");
        return dispatcher;
    }
    function releaseAsyncTransition() {
        ReactSharedInternals.asyncTransitions--;
    }
    function enqueueTask(task) {
        if (null === enqueueTaskImpl) try {
            var requireString = ("require" + Math.random()).slice(0, 7);
            enqueueTaskImpl = (module && module[requireString]).call(module, "timers").setImmediate;
        } catch (_err) {
            enqueueTaskImpl = function(callback) {
                !1 === didWarnAboutMessageChannel && (didWarnAboutMessageChannel = !0, "undefined" === typeof MessageChannel && console.error("This browser does not have a MessageChannel implementation, so enqueuing tasks via await act(async () => ...) will fail. Please file an issue at https://github.com/facebook/react/issues if you encounter this warning."));
                var channel = new MessageChannel();
                channel.port1.onmessage = callback;
                channel.port2.postMessage(void 0);
            };
        }
        return enqueueTaskImpl(task);
    }
    function aggregateErrors(errors) {
        return 1 < errors.length && "function" === typeof AggregateError ? new AggregateError(errors) : errors[0];
    }
    function popActScope(prevActQueue, prevActScopeDepth) {
        prevActScopeDepth !== actScopeDepth - 1 && console.error("You seem to have overlapping act() calls, this is not supported. Be sure to await previous act() calls before making a new one. ");
        actScopeDepth = prevActScopeDepth;
    }
    function recursivelyFlushAsyncActWork(returnValue, resolve, reject) {
        var queue = ReactSharedInternals.actQueue;
        if (null !== queue) if (0 !== queue.length) try {
            flushActQueue(queue);
            enqueueTask(function() {
                return recursivelyFlushAsyncActWork(returnValue, resolve, reject);
            });
            return;
        } catch (error) {
            ReactSharedInternals.thrownErrors.push(error);
        }
        else ReactSharedInternals.actQueue = null;
        0 < ReactSharedInternals.thrownErrors.length ? (queue = aggregateErrors(ReactSharedInternals.thrownErrors), ReactSharedInternals.thrownErrors.length = 0, reject(queue)) : resolve(returnValue);
    }
    function flushActQueue(queue) {
        if (!isFlushing) {
            isFlushing = !0;
            var i = 0;
            try {
                for(; i < queue.length; i++){
                    var callback = queue[i];
                    do {
                        ReactSharedInternals.didUsePromise = !1;
                        var continuation = callback(!1);
                        if (null !== continuation) {
                            if (ReactSharedInternals.didUsePromise) {
                                queue[i] = callback;
                                queue.splice(0, i);
                                return;
                            }
                            callback = continuation;
                        } else break;
                    }while (1)
                }
                queue.length = 0;
            } catch (error) {
                queue.splice(0, i + 1), ReactSharedInternals.thrownErrors.push(error);
            } finally{
                isFlushing = !1;
            }
        }
    }
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
    var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), MAYBE_ITERATOR_SYMBOL = Symbol.iterator, didWarnStateUpdateForUnmountedComponent = {}, ReactNoopUpdateQueue = {
        isMounted: function() {
            return !1;
        },
        enqueueForceUpdate: function(publicInstance) {
            warnNoop(publicInstance, "forceUpdate");
        },
        enqueueReplaceState: function(publicInstance) {
            warnNoop(publicInstance, "replaceState");
        },
        enqueueSetState: function(publicInstance) {
            warnNoop(publicInstance, "setState");
        }
    }, assign = Object.assign, emptyObject = {};
    Object.freeze(emptyObject);
    Component.prototype.isReactComponent = {};
    Component.prototype.setState = function(partialState, callback) {
        if ("object" !== typeof partialState && "function" !== typeof partialState && null != partialState) throw Error("takes an object of state variables to update or a function which returns an object of state variables.");
        this.updater.enqueueSetState(this, partialState, callback, "setState");
    };
    Component.prototype.forceUpdate = function(callback) {
        this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
    };
    var deprecatedAPIs = {
        isMounted: [
            "isMounted",
            "Instead, make sure to clean up subscriptions and pending requests in componentWillUnmount to prevent memory leaks."
        ],
        replaceState: [
            "replaceState",
            "Refactor your code to use setState instead (see https://github.com/facebook/react/issues/3236)."
        ]
    };
    for(fnName in deprecatedAPIs)deprecatedAPIs.hasOwnProperty(fnName) && defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    ComponentDummy.prototype = Component.prototype;
    deprecatedAPIs = PureComponent.prototype = new ComponentDummy();
    deprecatedAPIs.constructor = PureComponent;
    assign(deprecatedAPIs, Component.prototype);
    deprecatedAPIs.isPureReactComponent = !0;
    var isArrayImpl = Array.isArray, REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = {
        H: null,
        A: null,
        T: null,
        S: null,
        actQueue: null,
        asyncTransitions: 0,
        isBatchingLegacy: !1,
        didScheduleLegacyUpdate: !1,
        didUsePromise: !1,
        thrownErrors: [],
        getCurrentStack: null,
        recentlyCreatedOwnerStacks: 0
    }, hasOwnProperty = Object.prototype.hasOwnProperty, createTask = console.createTask ? console.createTask : function() {
        return null;
    };
    deprecatedAPIs = {
        react_stack_bottom_frame: function(callStackForError) {
            return callStackForError();
        }
    };
    var specialPropKeyWarningShown, didWarnAboutOldJSXRuntime;
    var didWarnAboutElementRef = {};
    var unknownOwnerDebugStack = deprecatedAPIs.react_stack_bottom_frame.bind(deprecatedAPIs, UnknownOwner)();
    var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
    var didWarnAboutMaps = !1, userProvidedKeyEscapeRegex = /\/+/g, reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
        if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
            var event = new window.ErrorEvent("error", {
                bubbles: !0,
                cancelable: !0,
                message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
                error: error
            });
            if (!window.dispatchEvent(event)) return;
        } else if ("object" === typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__["default"] && "function" === typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__["default"].emit) {
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__["default"].emit("uncaughtException", error);
            return;
        }
        console.error(error);
    }, didWarnAboutMessageChannel = !1, enqueueTaskImpl = null, actScopeDepth = 0, didWarnNoAwaitAct = !1, isFlushing = !1, queueSeveralMicrotasks = "function" === typeof queueMicrotask ? function(callback) {
        queueMicrotask(function() {
            return queueMicrotask(callback);
        });
    } : enqueueTask;
    deprecatedAPIs = Object.freeze({
        __proto__: null,
        c: function(size) {
            return resolveDispatcher().useMemoCache(size);
        }
    });
    var fnName = {
        map: mapChildren,
        forEach: function(children, forEachFunc, forEachContext) {
            mapChildren(children, function() {
                forEachFunc.apply(this, arguments);
            }, forEachContext);
        },
        count: function(children) {
            var n = 0;
            mapChildren(children, function() {
                n++;
            });
            return n;
        },
        toArray: function(children) {
            return mapChildren(children, function(child) {
                return child;
            }) || [];
        },
        only: function(children) {
            if (!isValidElement(children)) throw Error("React.Children.only expected to receive a single React element child.");
            return children;
        }
    };
    exports.Activity = REACT_ACTIVITY_TYPE;
    exports.Children = fnName;
    exports.Component = Component;
    exports.Fragment = REACT_FRAGMENT_TYPE;
    exports.Profiler = REACT_PROFILER_TYPE;
    exports.PureComponent = PureComponent;
    exports.StrictMode = REACT_STRICT_MODE_TYPE;
    exports.Suspense = REACT_SUSPENSE_TYPE;
    exports.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = ReactSharedInternals;
    exports.__COMPILER_RUNTIME = deprecatedAPIs;
    exports.act = function(callback) {
        var prevActQueue = ReactSharedInternals.actQueue, prevActScopeDepth = actScopeDepth;
        actScopeDepth++;
        var queue = ReactSharedInternals.actQueue = null !== prevActQueue ? prevActQueue : [], didAwaitActCall = !1;
        try {
            var result = callback();
        } catch (error) {
            ReactSharedInternals.thrownErrors.push(error);
        }
        if (0 < ReactSharedInternals.thrownErrors.length) throw popActScope(prevActQueue, prevActScopeDepth), callback = aggregateErrors(ReactSharedInternals.thrownErrors), ReactSharedInternals.thrownErrors.length = 0, callback;
        if (null !== result && "object" === typeof result && "function" === typeof result.then) {
            var thenable = result;
            queueSeveralMicrotasks(function() {
                didAwaitActCall || didWarnNoAwaitAct || (didWarnNoAwaitAct = !0, console.error("You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);"));
            });
            return {
                then: function(resolve, reject) {
                    didAwaitActCall = !0;
                    thenable.then(function(returnValue) {
                        popActScope(prevActQueue, prevActScopeDepth);
                        if (0 === prevActScopeDepth) {
                            try {
                                flushActQueue(queue), enqueueTask(function() {
                                    return recursivelyFlushAsyncActWork(returnValue, resolve, reject);
                                });
                            } catch (error$0) {
                                ReactSharedInternals.thrownErrors.push(error$0);
                            }
                            if (0 < ReactSharedInternals.thrownErrors.length) {
                                var _thrownError = aggregateErrors(ReactSharedInternals.thrownErrors);
                                ReactSharedInternals.thrownErrors.length = 0;
                                reject(_thrownError);
                            }
                        } else resolve(returnValue);
                    }, function(error) {
                        popActScope(prevActQueue, prevActScopeDepth);
                        0 < ReactSharedInternals.thrownErrors.length ? (error = aggregateErrors(ReactSharedInternals.thrownErrors), ReactSharedInternals.thrownErrors.length = 0, reject(error)) : reject(error);
                    });
                }
            };
        }
        var returnValue$jscomp$0 = result;
        popActScope(prevActQueue, prevActScopeDepth);
        0 === prevActScopeDepth && (flushActQueue(queue), 0 !== queue.length && queueSeveralMicrotasks(function() {
            didAwaitActCall || didWarnNoAwaitAct || (didWarnNoAwaitAct = !0, console.error("A component suspended inside an `act` scope, but the `act` call was not awaited. When testing React components that depend on asynchronous data, you must await the result:\n\nawait act(() => ...)"));
        }), ReactSharedInternals.actQueue = null);
        if (0 < ReactSharedInternals.thrownErrors.length) throw callback = aggregateErrors(ReactSharedInternals.thrownErrors), ReactSharedInternals.thrownErrors.length = 0, callback;
        return {
            then: function(resolve, reject) {
                didAwaitActCall = !0;
                0 === prevActScopeDepth ? (ReactSharedInternals.actQueue = queue, enqueueTask(function() {
                    return recursivelyFlushAsyncActWork(returnValue$jscomp$0, resolve, reject);
                })) : resolve(returnValue$jscomp$0);
            }
        };
    };
    exports.cache = function(fn) {
        return function() {
            return fn.apply(null, arguments);
        };
    };
    exports.cacheSignal = function() {
        return null;
    };
    exports.captureOwnerStack = function() {
        var getCurrentStack = ReactSharedInternals.getCurrentStack;
        return null === getCurrentStack ? null : getCurrentStack();
    };
    exports.cloneElement = function(element, config, children) {
        if (null === element || void 0 === element) throw Error("The argument must be a React element, but you passed " + element + ".");
        var props = assign({}, element.props), key = element.key, owner = element._owner;
        if (null != config) {
            var JSCompiler_inline_result;
            a: {
                if (hasOwnProperty.call(config, "ref") && (JSCompiler_inline_result = Object.getOwnPropertyDescriptor(config, "ref").get) && JSCompiler_inline_result.isReactWarning) {
                    JSCompiler_inline_result = !1;
                    break a;
                }
                JSCompiler_inline_result = void 0 !== config.ref;
            }
            JSCompiler_inline_result && (owner = getOwner());
            hasValidKey(config) && (checkKeyStringCoercion(config.key), key = "" + config.key);
            for(propName in config)!hasOwnProperty.call(config, propName) || "key" === propName || "__self" === propName || "__source" === propName || "ref" === propName && void 0 === config.ref || (props[propName] = config[propName]);
        }
        var propName = arguments.length - 2;
        if (1 === propName) props.children = children;
        else if (1 < propName) {
            JSCompiler_inline_result = Array(propName);
            for(var i = 0; i < propName; i++)JSCompiler_inline_result[i] = arguments[i + 2];
            props.children = JSCompiler_inline_result;
        }
        props = ReactElement(element.type, key, props, owner, element._debugStack, element._debugTask);
        for(key = 2; key < arguments.length; key++)validateChildKeys(arguments[key]);
        return props;
    };
    exports.createContext = function(defaultValue) {
        defaultValue = {
            $$typeof: REACT_CONTEXT_TYPE,
            _currentValue: defaultValue,
            _currentValue2: defaultValue,
            _threadCount: 0,
            Provider: null,
            Consumer: null
        };
        defaultValue.Provider = defaultValue;
        defaultValue.Consumer = {
            $$typeof: REACT_CONSUMER_TYPE,
            _context: defaultValue
        };
        defaultValue._currentRenderer = null;
        defaultValue._currentRenderer2 = null;
        return defaultValue;
    };
    exports.createElement = function(type, config, children) {
        for(var i = 2; i < arguments.length; i++)validateChildKeys(arguments[i]);
        i = {};
        var key = null;
        if (null != config) for(propName in didWarnAboutOldJSXRuntime || !("__self" in config) || "key" in config || (didWarnAboutOldJSXRuntime = !0, console.warn("Your app (or one of its dependencies) is using an outdated JSX transform. Update to the modern JSX transform for faster performance: https://react.dev/link/new-jsx-transform")), hasValidKey(config) && (checkKeyStringCoercion(config.key), key = "" + config.key), config)hasOwnProperty.call(config, propName) && "key" !== propName && "__self" !== propName && "__source" !== propName && (i[propName] = config[propName]);
        var childrenLength = arguments.length - 2;
        if (1 === childrenLength) i.children = children;
        else if (1 < childrenLength) {
            for(var childArray = Array(childrenLength), _i = 0; _i < childrenLength; _i++)childArray[_i] = arguments[_i + 2];
            Object.freeze && Object.freeze(childArray);
            i.children = childArray;
        }
        if (type && type.defaultProps) for(propName in childrenLength = type.defaultProps, childrenLength)void 0 === i[propName] && (i[propName] = childrenLength[propName]);
        key && defineKeyPropWarningGetter(i, "function" === typeof type ? type.displayName || type.name || "Unknown" : type);
        var propName = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return ReactElement(type, key, i, getOwner(), propName ? Error("react-stack-top-frame") : unknownOwnerDebugStack, propName ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
    };
    exports.createRef = function() {
        var refObject = {
            current: null
        };
        Object.seal(refObject);
        return refObject;
    };
    exports.forwardRef = function(render) {
        null != render && render.$$typeof === REACT_MEMO_TYPE ? console.error("forwardRef requires a render function but received a `memo` component. Instead of forwardRef(memo(...)), use memo(forwardRef(...)).") : "function" !== typeof render ? console.error("forwardRef requires a render function but was given %s.", null === render ? "null" : typeof render) : 0 !== render.length && 2 !== render.length && console.error("forwardRef render functions accept exactly two parameters: props and ref. %s", 1 === render.length ? "Did you forget to use the ref parameter?" : "Any additional parameter will be undefined.");
        null != render && null != render.defaultProps && console.error("forwardRef render functions do not support defaultProps. Did you accidentally pass a React component?");
        var elementType = {
            $$typeof: REACT_FORWARD_REF_TYPE,
            render: render
        }, ownName;
        Object.defineProperty(elementType, "displayName", {
            enumerable: !1,
            configurable: !0,
            get: function() {
                return ownName;
            },
            set: function(name) {
                ownName = name;
                render.name || render.displayName || (Object.defineProperty(render, "name", {
                    value: name
                }), render.displayName = name);
            }
        });
        return elementType;
    };
    exports.isValidElement = isValidElement;
    exports.lazy = function(ctor) {
        ctor = {
            _status: -1,
            _result: ctor
        };
        var lazyType = {
            $$typeof: REACT_LAZY_TYPE,
            _payload: ctor,
            _init: lazyInitializer
        }, ioInfo = {
            name: "lazy",
            start: -1,
            end: -1,
            value: null,
            owner: null,
            debugStack: Error("react-stack-top-frame"),
            debugTask: console.createTask ? console.createTask("lazy()") : null
        };
        ctor._ioInfo = ioInfo;
        lazyType._debugInfo = [
            {
                awaited: ioInfo
            }
        ];
        return lazyType;
    };
    exports.memo = function(type, compare) {
        null == type && console.error("memo: The first argument must be a component. Instead received: %s", null === type ? "null" : typeof type);
        compare = {
            $$typeof: REACT_MEMO_TYPE,
            type: type,
            compare: void 0 === compare ? null : compare
        };
        var ownName;
        Object.defineProperty(compare, "displayName", {
            enumerable: !1,
            configurable: !0,
            get: function() {
                return ownName;
            },
            set: function(name) {
                ownName = name;
                type.name || type.displayName || (Object.defineProperty(type, "name", {
                    value: name
                }), type.displayName = name);
            }
        });
        return compare;
    };
    exports.startTransition = function(scope) {
        var prevTransition = ReactSharedInternals.T, currentTransition = {};
        currentTransition._updatedFibers = new Set();
        ReactSharedInternals.T = currentTransition;
        try {
            var returnValue = scope(), onStartTransitionFinish = ReactSharedInternals.S;
            null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
            "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && (ReactSharedInternals.asyncTransitions++, returnValue.then(releaseAsyncTransition, releaseAsyncTransition), returnValue.then(noop, reportGlobalError));
        } catch (error) {
            reportGlobalError(error);
        } finally{
            null === prevTransition && currentTransition._updatedFibers && (scope = currentTransition._updatedFibers.size, currentTransition._updatedFibers.clear(), 10 < scope && console.warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table.")), null !== prevTransition && null !== currentTransition.types && (null !== prevTransition.types && prevTransition.types !== currentTransition.types && console.error("We expected inner Transitions to have transferred the outer types set and that you cannot add to the outer Transition while inside the inner.This is a bug in React."), prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
        }
    };
    exports.unstable_useCacheRefresh = function() {
        return resolveDispatcher().useCacheRefresh();
    };
    exports.use = function(usable) {
        return resolveDispatcher().use(usable);
    };
    exports.useActionState = function(action, initialState, permalink) {
        return resolveDispatcher().useActionState(action, initialState, permalink);
    };
    exports.useCallback = function(callback, deps) {
        return resolveDispatcher().useCallback(callback, deps);
    };
    exports.useContext = function(Context) {
        var dispatcher = resolveDispatcher();
        Context.$$typeof === REACT_CONSUMER_TYPE && console.error("Calling useContext(Context.Consumer) is not supported and will cause bugs. Did you mean to call useContext(Context) instead?");
        return dispatcher.useContext(Context);
    };
    exports.useDebugValue = function(value, formatterFn) {
        return resolveDispatcher().useDebugValue(value, formatterFn);
    };
    exports.useDeferredValue = function(value, initialValue) {
        return resolveDispatcher().useDeferredValue(value, initialValue);
    };
    exports.useEffect = function(create, deps) {
        null == create && console.warn("React Hook useEffect requires an effect callback. Did you forget to pass a callback to the hook?");
        return resolveDispatcher().useEffect(create, deps);
    };
    exports.useEffectEvent = function(callback) {
        return resolveDispatcher().useEffectEvent(callback);
    };
    exports.useId = function() {
        return resolveDispatcher().useId();
    };
    exports.useImperativeHandle = function(ref, create, deps) {
        return resolveDispatcher().useImperativeHandle(ref, create, deps);
    };
    exports.useInsertionEffect = function(create, deps) {
        null == create && console.warn("React Hook useInsertionEffect requires an effect callback. Did you forget to pass a callback to the hook?");
        return resolveDispatcher().useInsertionEffect(create, deps);
    };
    exports.useLayoutEffect = function(create, deps) {
        null == create && console.warn("React Hook useLayoutEffect requires an effect callback. Did you forget to pass a callback to the hook?");
        return resolveDispatcher().useLayoutEffect(create, deps);
    };
    exports.useMemo = function(create, deps) {
        return resolveDispatcher().useMemo(create, deps);
    };
    exports.useOptimistic = function(passthrough, reducer) {
        return resolveDispatcher().useOptimistic(passthrough, reducer);
    };
    exports.useReducer = function(reducer, initialArg, init) {
        return resolveDispatcher().useReducer(reducer, initialArg, init);
    };
    exports.useRef = function(initialValue) {
        return resolveDispatcher().useRef(initialValue);
    };
    exports.useState = function(initialState) {
        return resolveDispatcher().useState(initialState);
    };
    exports.useSyncExternalStore = function(subscribe, getSnapshot, getServerSnapshot) {
        return resolveDispatcher().useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
    };
    exports.useTransition = function() {
        return resolveDispatcher().useTransition();
    };
    exports.version = "19.2.0";
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
}();
}),
"[project]/node_modules/react/index.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
'use strict';
if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
;
else {
    module.exports = __turbopack_context__.r("[project]/node_modules/react/cjs/react.development.js [client] (ecmascript)");
}
}),
"[project]/node_modules/react/cjs/react-jsx-runtime.development.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
"use strict";
"production" !== ("TURBOPACK compile-time value", "development") && function() {
    function getComponentNameFromType(type) {
        if (null == type) return null;
        if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
        if ("string" === typeof type) return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return "Fragment";
            case REACT_PROFILER_TYPE:
                return "Profiler";
            case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
            case REACT_SUSPENSE_TYPE:
                return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            case REACT_ACTIVITY_TYPE:
                return "Activity";
        }
        if ("object" === typeof type) switch("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof){
            case REACT_PORTAL_TYPE:
                return "Portal";
            case REACT_CONTEXT_TYPE:
                return type.displayName || "Context";
            case REACT_CONSUMER_TYPE:
                return (type._context.displayName || "Context") + ".Consumer";
            case REACT_FORWARD_REF_TYPE:
                var innerType = type.render;
                type = type.displayName;
                type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
                return type;
            case REACT_MEMO_TYPE:
                return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
            case REACT_LAZY_TYPE:
                innerType = type._payload;
                type = type._init;
                try {
                    return getComponentNameFromType(type(innerType));
                } catch (x) {}
        }
        return null;
    }
    function testStringCoercion(value) {
        return "" + value;
    }
    function checkKeyStringCoercion(value) {
        try {
            testStringCoercion(value);
            var JSCompiler_inline_result = !1;
        } catch (e) {
            JSCompiler_inline_result = !0;
        }
        if (JSCompiler_inline_result) {
            JSCompiler_inline_result = console;
            var JSCompiler_temp_const = JSCompiler_inline_result.error;
            var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            JSCompiler_temp_const.call(JSCompiler_inline_result, "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.", JSCompiler_inline_result$jscomp$0);
            return testStringCoercion(value);
        }
    }
    function getTaskName(type) {
        if (type === REACT_FRAGMENT_TYPE) return "<>";
        if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE) return "<...>";
        try {
            var name = getComponentNameFromType(type);
            return name ? "<" + name + ">" : "<...>";
        } catch (x) {
            return "<...>";
        }
    }
    function getOwner() {
        var dispatcher = ReactSharedInternals.A;
        return null === dispatcher ? null : dispatcher.getOwner();
    }
    function UnknownOwner() {
        return Error("react-stack-top-frame");
    }
    function hasValidKey(config) {
        if (hasOwnProperty.call(config, "key")) {
            var getter = Object.getOwnPropertyDescriptor(config, "key").get;
            if (getter && getter.isReactWarning) return !1;
        }
        return void 0 !== config.key;
    }
    function defineKeyPropWarningGetter(props, displayName) {
        function warnAboutAccessingKey() {
            specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, console.error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)", displayName));
        }
        warnAboutAccessingKey.isReactWarning = !0;
        Object.defineProperty(props, "key", {
            get: warnAboutAccessingKey,
            configurable: !0
        });
    }
    function elementRefGetterWithDeprecationWarning() {
        var componentName = getComponentNameFromType(this.type);
        didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = !0, console.error("Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."));
        componentName = this.props.ref;
        return void 0 !== componentName ? componentName : null;
    }
    function ReactElement(type, key, props, owner, debugStack, debugTask) {
        var refProp = props.ref;
        type = {
            $$typeof: REACT_ELEMENT_TYPE,
            type: type,
            key: key,
            props: props,
            _owner: owner
        };
        null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
            enumerable: !1,
            get: elementRefGetterWithDeprecationWarning
        }) : Object.defineProperty(type, "ref", {
            enumerable: !1,
            value: null
        });
        type._store = {};
        Object.defineProperty(type._store, "validated", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: 0
        });
        Object.defineProperty(type, "_debugInfo", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: null
        });
        Object.defineProperty(type, "_debugStack", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugStack
        });
        Object.defineProperty(type, "_debugTask", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugTask
        });
        Object.freeze && (Object.freeze(type.props), Object.freeze(type));
        return type;
    }
    function jsxDEVImpl(type, config, maybeKey, isStaticChildren, debugStack, debugTask) {
        var children = config.children;
        if (void 0 !== children) if (isStaticChildren) if (isArrayImpl(children)) {
            for(isStaticChildren = 0; isStaticChildren < children.length; isStaticChildren++)validateChildKeys(children[isStaticChildren]);
            Object.freeze && Object.freeze(children);
        } else console.error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
        else validateChildKeys(children);
        if (hasOwnProperty.call(config, "key")) {
            children = getComponentNameFromType(type);
            var keys = Object.keys(config).filter(function(k) {
                return "key" !== k;
            });
            isStaticChildren = 0 < keys.length ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
            didWarnAboutKeySpread[children + isStaticChildren] || (keys = 0 < keys.length ? "{" + keys.join(": ..., ") + ": ...}" : "{}", console.error('A props object containing a "key" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />', isStaticChildren, children, keys, children), didWarnAboutKeySpread[children + isStaticChildren] = !0);
        }
        children = null;
        void 0 !== maybeKey && (checkKeyStringCoercion(maybeKey), children = "" + maybeKey);
        hasValidKey(config) && (checkKeyStringCoercion(config.key), children = "" + config.key);
        if ("key" in config) {
            maybeKey = {};
            for(var propName in config)"key" !== propName && (maybeKey[propName] = config[propName]);
        } else maybeKey = config;
        children && defineKeyPropWarningGetter(maybeKey, "function" === typeof type ? type.displayName || type.name || "Unknown" : type);
        return ReactElement(type, children, maybeKey, getOwner(), debugStack, debugTask);
    }
    function validateChildKeys(node) {
        isValidElement(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
    }
    function isValidElement(object) {
        return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    var React = __turbopack_context__.r("[project]/node_modules/react/index.js [client] (ecmascript)"), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, hasOwnProperty = Object.prototype.hasOwnProperty, isArrayImpl = Array.isArray, createTask = console.createTask ? console.createTask : function() {
        return null;
    };
    React = {
        react_stack_bottom_frame: function(callStackForError) {
            return callStackForError();
        }
    };
    var specialPropKeyWarningShown;
    var didWarnAboutElementRef = {};
    var unknownOwnerDebugStack = React.react_stack_bottom_frame.bind(React, UnknownOwner)();
    var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
    var didWarnAboutKeySpread = {};
    exports.Fragment = REACT_FRAGMENT_TYPE;
    exports.jsx = function(type, config, maybeKey) {
        var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return jsxDEVImpl(type, config, maybeKey, !1, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
    };
    exports.jsxs = function(type, config, maybeKey) {
        var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return jsxDEVImpl(type, config, maybeKey, !0, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
    };
}();
}),
"[project]/node_modules/react/jsx-runtime.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
'use strict';
if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
;
else {
    module.exports = __turbopack_context__.r("[project]/node_modules/react/cjs/react-jsx-runtime.development.js [client] (ecmascript)");
}
}),
"[project]/node_modules/react/cjs/react-jsx-dev-runtime.development.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * @license React
 * react-jsx-dev-runtime.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
"use strict";
"production" !== ("TURBOPACK compile-time value", "development") && function() {
    function getComponentNameFromType(type) {
        if (null == type) return null;
        if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
        if ("string" === typeof type) return type;
        switch(type){
            case REACT_FRAGMENT_TYPE:
                return "Fragment";
            case REACT_PROFILER_TYPE:
                return "Profiler";
            case REACT_STRICT_MODE_TYPE:
                return "StrictMode";
            case REACT_SUSPENSE_TYPE:
                return "Suspense";
            case REACT_SUSPENSE_LIST_TYPE:
                return "SuspenseList";
            case REACT_ACTIVITY_TYPE:
                return "Activity";
        }
        if ("object" === typeof type) switch("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof){
            case REACT_PORTAL_TYPE:
                return "Portal";
            case REACT_CONTEXT_TYPE:
                return type.displayName || "Context";
            case REACT_CONSUMER_TYPE:
                return (type._context.displayName || "Context") + ".Consumer";
            case REACT_FORWARD_REF_TYPE:
                var innerType = type.render;
                type = type.displayName;
                type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
                return type;
            case REACT_MEMO_TYPE:
                return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
            case REACT_LAZY_TYPE:
                innerType = type._payload;
                type = type._init;
                try {
                    return getComponentNameFromType(type(innerType));
                } catch (x) {}
        }
        return null;
    }
    function testStringCoercion(value) {
        return "" + value;
    }
    function checkKeyStringCoercion(value) {
        try {
            testStringCoercion(value);
            var JSCompiler_inline_result = !1;
        } catch (e) {
            JSCompiler_inline_result = !0;
        }
        if (JSCompiler_inline_result) {
            JSCompiler_inline_result = console;
            var JSCompiler_temp_const = JSCompiler_inline_result.error;
            var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
            JSCompiler_temp_const.call(JSCompiler_inline_result, "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.", JSCompiler_inline_result$jscomp$0);
            return testStringCoercion(value);
        }
    }
    function getTaskName(type) {
        if (type === REACT_FRAGMENT_TYPE) return "<>";
        if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE) return "<...>";
        try {
            var name = getComponentNameFromType(type);
            return name ? "<" + name + ">" : "<...>";
        } catch (x) {
            return "<...>";
        }
    }
    function getOwner() {
        var dispatcher = ReactSharedInternals.A;
        return null === dispatcher ? null : dispatcher.getOwner();
    }
    function UnknownOwner() {
        return Error("react-stack-top-frame");
    }
    function hasValidKey(config) {
        if (hasOwnProperty.call(config, "key")) {
            var getter = Object.getOwnPropertyDescriptor(config, "key").get;
            if (getter && getter.isReactWarning) return !1;
        }
        return void 0 !== config.key;
    }
    function defineKeyPropWarningGetter(props, displayName) {
        function warnAboutAccessingKey() {
            specialPropKeyWarningShown || (specialPropKeyWarningShown = !0, console.error("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)", displayName));
        }
        warnAboutAccessingKey.isReactWarning = !0;
        Object.defineProperty(props, "key", {
            get: warnAboutAccessingKey,
            configurable: !0
        });
    }
    function elementRefGetterWithDeprecationWarning() {
        var componentName = getComponentNameFromType(this.type);
        didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = !0, console.error("Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."));
        componentName = this.props.ref;
        return void 0 !== componentName ? componentName : null;
    }
    function ReactElement(type, key, props, owner, debugStack, debugTask) {
        var refProp = props.ref;
        type = {
            $$typeof: REACT_ELEMENT_TYPE,
            type: type,
            key: key,
            props: props,
            _owner: owner
        };
        null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
            enumerable: !1,
            get: elementRefGetterWithDeprecationWarning
        }) : Object.defineProperty(type, "ref", {
            enumerable: !1,
            value: null
        });
        type._store = {};
        Object.defineProperty(type._store, "validated", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: 0
        });
        Object.defineProperty(type, "_debugInfo", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: null
        });
        Object.defineProperty(type, "_debugStack", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugStack
        });
        Object.defineProperty(type, "_debugTask", {
            configurable: !1,
            enumerable: !1,
            writable: !0,
            value: debugTask
        });
        Object.freeze && (Object.freeze(type.props), Object.freeze(type));
        return type;
    }
    function jsxDEVImpl(type, config, maybeKey, isStaticChildren, debugStack, debugTask) {
        var children = config.children;
        if (void 0 !== children) if (isStaticChildren) if (isArrayImpl(children)) {
            for(isStaticChildren = 0; isStaticChildren < children.length; isStaticChildren++)validateChildKeys(children[isStaticChildren]);
            Object.freeze && Object.freeze(children);
        } else console.error("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
        else validateChildKeys(children);
        if (hasOwnProperty.call(config, "key")) {
            children = getComponentNameFromType(type);
            var keys = Object.keys(config).filter(function(k) {
                return "key" !== k;
            });
            isStaticChildren = 0 < keys.length ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
            didWarnAboutKeySpread[children + isStaticChildren] || (keys = 0 < keys.length ? "{" + keys.join(": ..., ") + ": ...}" : "{}", console.error('A props object containing a "key" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />', isStaticChildren, children, keys, children), didWarnAboutKeySpread[children + isStaticChildren] = !0);
        }
        children = null;
        void 0 !== maybeKey && (checkKeyStringCoercion(maybeKey), children = "" + maybeKey);
        hasValidKey(config) && (checkKeyStringCoercion(config.key), children = "" + config.key);
        if ("key" in config) {
            maybeKey = {};
            for(var propName in config)"key" !== propName && (maybeKey[propName] = config[propName]);
        } else maybeKey = config;
        children && defineKeyPropWarningGetter(maybeKey, "function" === typeof type ? type.displayName || type.name || "Unknown" : type);
        return ReactElement(type, children, maybeKey, getOwner(), debugStack, debugTask);
    }
    function validateChildKeys(node) {
        isValidElement(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
    }
    function isValidElement(object) {
        return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
    }
    var React = __turbopack_context__.r("[project]/node_modules/react/index.js [client] (ecmascript)"), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, hasOwnProperty = Object.prototype.hasOwnProperty, isArrayImpl = Array.isArray, createTask = console.createTask ? console.createTask : function() {
        return null;
    };
    React = {
        react_stack_bottom_frame: function(callStackForError) {
            return callStackForError();
        }
    };
    var specialPropKeyWarningShown;
    var didWarnAboutElementRef = {};
    var unknownOwnerDebugStack = React.react_stack_bottom_frame.bind(React, UnknownOwner)();
    var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
    var didWarnAboutKeySpread = {};
    exports.Fragment = REACT_FRAGMENT_TYPE;
    exports.jsxDEV = function(type, config, maybeKey, isStaticChildren) {
        var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return jsxDEVImpl(type, config, maybeKey, isStaticChildren, trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack, trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask);
    };
}();
}),
"[project]/node_modules/react/jsx-dev-runtime.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
'use strict';
if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
;
else {
    module.exports = __turbopack_context__.r("[project]/node_modules/react/cjs/react-jsx-dev-runtime.development.js [client] (ecmascript)");
}
}),
"[project]/node_modules/scheduler/cjs/scheduler.development.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * @license React
 * scheduler.development.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
"use strict";
"production" !== ("TURBOPACK compile-time value", "development") && function() {
    function performWorkUntilDeadline() {
        needsPaint = !1;
        if (isMessageLoopRunning) {
            var currentTime = exports.unstable_now();
            startTime = currentTime;
            var hasMoreWork = !0;
            try {
                a: {
                    isHostCallbackScheduled = !1;
                    isHostTimeoutScheduled && (isHostTimeoutScheduled = !1, localClearTimeout(taskTimeoutID), taskTimeoutID = -1);
                    isPerformingWork = !0;
                    var previousPriorityLevel = currentPriorityLevel;
                    try {
                        b: {
                            advanceTimers(currentTime);
                            for(currentTask = peek(taskQueue); null !== currentTask && !(currentTask.expirationTime > currentTime && shouldYieldToHost());){
                                var callback = currentTask.callback;
                                if ("function" === typeof callback) {
                                    currentTask.callback = null;
                                    currentPriorityLevel = currentTask.priorityLevel;
                                    var continuationCallback = callback(currentTask.expirationTime <= currentTime);
                                    currentTime = exports.unstable_now();
                                    if ("function" === typeof continuationCallback) {
                                        currentTask.callback = continuationCallback;
                                        advanceTimers(currentTime);
                                        hasMoreWork = !0;
                                        break b;
                                    }
                                    currentTask === peek(taskQueue) && pop(taskQueue);
                                    advanceTimers(currentTime);
                                } else pop(taskQueue);
                                currentTask = peek(taskQueue);
                            }
                            if (null !== currentTask) hasMoreWork = !0;
                            else {
                                var firstTimer = peek(timerQueue);
                                null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
                                hasMoreWork = !1;
                            }
                        }
                        break a;
                    } finally{
                        currentTask = null, currentPriorityLevel = previousPriorityLevel, isPerformingWork = !1;
                    }
                    hasMoreWork = void 0;
                }
            } finally{
                hasMoreWork ? schedulePerformWorkUntilDeadline() : isMessageLoopRunning = !1;
            }
        }
    }
    function push(heap, node) {
        var index = heap.length;
        heap.push(node);
        a: for(; 0 < index;){
            var parentIndex = index - 1 >>> 1, parent = heap[parentIndex];
            if (0 < compare(parent, node)) heap[parentIndex] = node, heap[index] = parent, index = parentIndex;
            else break a;
        }
    }
    function peek(heap) {
        return 0 === heap.length ? null : heap[0];
    }
    function pop(heap) {
        if (0 === heap.length) return null;
        var first = heap[0], last = heap.pop();
        if (last !== first) {
            heap[0] = last;
            a: for(var index = 0, length = heap.length, halfLength = length >>> 1; index < halfLength;){
                var leftIndex = 2 * (index + 1) - 1, left = heap[leftIndex], rightIndex = leftIndex + 1, right = heap[rightIndex];
                if (0 > compare(left, last)) rightIndex < length && 0 > compare(right, left) ? (heap[index] = right, heap[rightIndex] = last, index = rightIndex) : (heap[index] = left, heap[leftIndex] = last, index = leftIndex);
                else if (rightIndex < length && 0 > compare(right, last)) heap[index] = right, heap[rightIndex] = last, index = rightIndex;
                else break a;
            }
        }
        return first;
    }
    function compare(a, b) {
        var diff = a.sortIndex - b.sortIndex;
        return 0 !== diff ? diff : a.id - b.id;
    }
    function advanceTimers(currentTime) {
        for(var timer = peek(timerQueue); null !== timer;){
            if (null === timer.callback) pop(timerQueue);
            else if (timer.startTime <= currentTime) pop(timerQueue), timer.sortIndex = timer.expirationTime, push(taskQueue, timer);
            else break;
            timer = peek(timerQueue);
        }
    }
    function handleTimeout(currentTime) {
        isHostTimeoutScheduled = !1;
        advanceTimers(currentTime);
        if (!isHostCallbackScheduled) if (null !== peek(taskQueue)) isHostCallbackScheduled = !0, isMessageLoopRunning || (isMessageLoopRunning = !0, schedulePerformWorkUntilDeadline());
        else {
            var firstTimer = peek(timerQueue);
            null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
        }
    }
    function shouldYieldToHost() {
        return needsPaint ? !0 : exports.unstable_now() - startTime < frameInterval ? !1 : !0;
    }
    function requestHostTimeout(callback, ms) {
        taskTimeoutID = localSetTimeout(function() {
            callback(exports.unstable_now());
        }, ms);
    }
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
    exports.unstable_now = void 0;
    if ("object" === typeof performance && "function" === typeof performance.now) {
        var localPerformance = performance;
        exports.unstable_now = function() {
            return localPerformance.now();
        };
    } else {
        var localDate = Date, initialTime = localDate.now();
        exports.unstable_now = function() {
            return localDate.now() - initialTime;
        };
    }
    var taskQueue = [], timerQueue = [], taskIdCounter = 1, currentTask = null, currentPriorityLevel = 3, isPerformingWork = !1, isHostCallbackScheduled = !1, isHostTimeoutScheduled = !1, needsPaint = !1, localSetTimeout = "function" === typeof setTimeout ? setTimeout : null, localClearTimeout = "function" === typeof clearTimeout ? clearTimeout : null, localSetImmediate = "undefined" !== typeof setImmediate ? setImmediate : null, isMessageLoopRunning = !1, taskTimeoutID = -1, frameInterval = 5, startTime = -1;
    if ("function" === typeof localSetImmediate) var schedulePerformWorkUntilDeadline = function() {
        localSetImmediate(performWorkUntilDeadline);
    };
    else if ("undefined" !== typeof MessageChannel) {
        var channel = new MessageChannel(), port = channel.port2;
        channel.port1.onmessage = performWorkUntilDeadline;
        schedulePerformWorkUntilDeadline = function() {
            port.postMessage(null);
        };
    } else schedulePerformWorkUntilDeadline = function() {
        localSetTimeout(performWorkUntilDeadline, 0);
    };
    exports.unstable_IdlePriority = 5;
    exports.unstable_ImmediatePriority = 1;
    exports.unstable_LowPriority = 4;
    exports.unstable_NormalPriority = 3;
    exports.unstable_Profiling = null;
    exports.unstable_UserBlockingPriority = 2;
    exports.unstable_cancelCallback = function(task) {
        task.callback = null;
    };
    exports.unstable_forceFrameRate = function(fps) {
        0 > fps || 125 < fps ? console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported") : frameInterval = 0 < fps ? Math.floor(1e3 / fps) : 5;
    };
    exports.unstable_getCurrentPriorityLevel = function() {
        return currentPriorityLevel;
    };
    exports.unstable_next = function(eventHandler) {
        switch(currentPriorityLevel){
            case 1:
            case 2:
            case 3:
                var priorityLevel = 3;
                break;
            default:
                priorityLevel = currentPriorityLevel;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
            return eventHandler();
        } finally{
            currentPriorityLevel = previousPriorityLevel;
        }
    };
    exports.unstable_requestPaint = function() {
        needsPaint = !0;
    };
    exports.unstable_runWithPriority = function(priorityLevel, eventHandler) {
        switch(priorityLevel){
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
                break;
            default:
                priorityLevel = 3;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
            return eventHandler();
        } finally{
            currentPriorityLevel = previousPriorityLevel;
        }
    };
    exports.unstable_scheduleCallback = function(priorityLevel, callback, options) {
        var currentTime = exports.unstable_now();
        "object" === typeof options && null !== options ? (options = options.delay, options = "number" === typeof options && 0 < options ? currentTime + options : currentTime) : options = currentTime;
        switch(priorityLevel){
            case 1:
                var timeout = -1;
                break;
            case 2:
                timeout = 250;
                break;
            case 5:
                timeout = 1073741823;
                break;
            case 4:
                timeout = 1e4;
                break;
            default:
                timeout = 5e3;
        }
        timeout = options + timeout;
        priorityLevel = {
            id: taskIdCounter++,
            callback: callback,
            priorityLevel: priorityLevel,
            startTime: options,
            expirationTime: timeout,
            sortIndex: -1
        };
        options > currentTime ? (priorityLevel.sortIndex = options, push(timerQueue, priorityLevel), null === peek(taskQueue) && priorityLevel === peek(timerQueue) && (isHostTimeoutScheduled ? (localClearTimeout(taskTimeoutID), taskTimeoutID = -1) : isHostTimeoutScheduled = !0, requestHostTimeout(handleTimeout, options - currentTime))) : (priorityLevel.sortIndex = timeout, push(taskQueue, priorityLevel), isHostCallbackScheduled || isPerformingWork || (isHostCallbackScheduled = !0, isMessageLoopRunning || (isMessageLoopRunning = !0, schedulePerformWorkUntilDeadline())));
        return priorityLevel;
    };
    exports.unstable_shouldYield = shouldYieldToHost;
    exports.unstable_wrapCallback = function(callback) {
        var parentPriorityLevel = currentPriorityLevel;
        return function() {
            var previousPriorityLevel = currentPriorityLevel;
            currentPriorityLevel = parentPriorityLevel;
            try {
                return callback.apply(this, arguments);
            } finally{
                currentPriorityLevel = previousPriorityLevel;
            }
        };
    };
    "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
}();
}),
"[project]/node_modules/scheduler/index.js [client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
'use strict';
if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
;
else {
    module.exports = __turbopack_context__.r("[project]/node_modules/scheduler/cjs/scheduler.development.js [client] (ecmascript)");
}
}),
"[project]/node_modules/motion-utils/dist/es/clamp.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "clamp",
    ()=>clamp
]);
const clamp = (min, max, v)=>{
    if (v > max) return max;
    if (v < min) return min;
    return v;
};
;
}),
"[project]/node_modules/motion-utils/dist/es/format-error-message.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "formatErrorMessage",
    ()=>formatErrorMessage
]);
function formatErrorMessage(message, errorCode) {
    return errorCode ? `${message}. For more information and steps for solving, visit https://motion.dev/troubleshooting/${errorCode}` : message;
}
;
}),
"[project]/node_modules/motion-utils/dist/es/errors.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "invariant",
    ()=>invariant,
    "warning",
    ()=>warning
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$build$2f$polyfills$2f$process$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/build/polyfills/process.js [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$format$2d$error$2d$message$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/format-error-message.mjs [client] (ecmascript)");
;
let warning = ()=>{};
let invariant = ()=>{};
if ("TURBOPACK compile-time truthy", 1) {
    warning = (check, message, errorCode)=>{
        if (!check && typeof console !== "undefined") {
            console.warn((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$format$2d$error$2d$message$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["formatErrorMessage"])(message, errorCode));
        }
    };
    invariant = (check, message, errorCode)=>{
        if (!check) {
            throw new Error((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$format$2d$error$2d$message$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["formatErrorMessage"])(message, errorCode));
        }
    };
}
;
}),
"[project]/node_modules/motion-utils/dist/es/is-numerical-string.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Check if value is a numerical string, ie a string that is purely a number eg "100" or "-100.1"
 */ __turbopack_context__.s([
    "isNumericalString",
    ()=>isNumericalString
]);
const isNumericalString = (v)=>/^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(v);
;
}),
"[project]/node_modules/motion-utils/dist/es/noop.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/*#__NO_SIDE_EFFECTS__*/ __turbopack_context__.s([
    "noop",
    ()=>noop
]);
const noop = (any)=>any;
;
}),
"[project]/node_modules/motion-utils/dist/es/global-config.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MotionGlobalConfig",
    ()=>MotionGlobalConfig
]);
const MotionGlobalConfig = {};
;
}),
"[project]/node_modules/motion-utils/dist/es/is-zero-value-string.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Check if the value is a zero value string like "0px" or "0%"
 */ __turbopack_context__.s([
    "isZeroValueString",
    ()=>isZeroValueString
]);
const isZeroValueString = (v)=>/^0[^.\s]+$/u.test(v);
;
}),
"[project]/node_modules/motion-utils/dist/es/warn-once.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "hasWarned",
    ()=>hasWarned,
    "warnOnce",
    ()=>warnOnce
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$format$2d$error$2d$message$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/format-error-message.mjs [client] (ecmascript)");
;
const warned = new Set();
function hasWarned(message) {
    return warned.has(message);
}
function warnOnce(condition, message, errorCode) {
    if (condition || warned.has(message)) return;
    console.warn((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$format$2d$error$2d$message$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["formatErrorMessage"])(message, errorCode));
    warned.add(message);
}
;
}),
"[project]/node_modules/motion-utils/dist/es/array.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addUniqueItem",
    ()=>addUniqueItem,
    "moveItem",
    ()=>moveItem,
    "removeItem",
    ()=>removeItem
]);
function addUniqueItem(arr, item) {
    if (arr.indexOf(item) === -1) arr.push(item);
}
function removeItem(arr, item) {
    const index = arr.indexOf(item);
    if (index > -1) arr.splice(index, 1);
}
// Adapted from array-move
function moveItem([...arr], fromIndex, toIndex) {
    const startIndex = fromIndex < 0 ? arr.length + fromIndex : fromIndex;
    if (startIndex >= 0 && startIndex < arr.length) {
        const endIndex = toIndex < 0 ? arr.length + toIndex : toIndex;
        const [item] = arr.splice(fromIndex, 1);
        arr.splice(endIndex, 0, item);
    }
    return arr;
}
;
}),
"[project]/node_modules/motion-utils/dist/es/subscription-manager.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SubscriptionManager",
    ()=>SubscriptionManager
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/array.mjs [client] (ecmascript)");
;
class SubscriptionManager {
    constructor(){
        this.subscriptions = [];
    }
    add(handler) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["addUniqueItem"])(this.subscriptions, handler);
        return ()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$array$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["removeItem"])(this.subscriptions, handler);
    }
    notify(a, b, c) {
        const numSubscriptions = this.subscriptions.length;
        if (!numSubscriptions) return;
        if (numSubscriptions === 1) {
            /**
             * If there's only a single handler we can just call it without invoking a loop.
             */ this.subscriptions[0](a, b, c);
        } else {
            for(let i = 0; i < numSubscriptions; i++){
                /**
                 * Check whether the handler exists before firing as it's possible
                 * the subscriptions were modified during this loop running.
                 */ const handler = this.subscriptions[i];
                handler && handler(a, b, c);
            }
        }
    }
    getSize() {
        return this.subscriptions.length;
    }
    clear() {
        this.subscriptions.length = 0;
    }
}
;
}),
"[project]/node_modules/motion-utils/dist/es/velocity-per-second.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/*
  Convert velocity into velocity per second

  @param [number]: Unit per frame
  @param [number]: Frame duration in ms
*/ __turbopack_context__.s([
    "velocityPerSecond",
    ()=>velocityPerSecond
]);
function velocityPerSecond(velocity, frameDuration) {
    return frameDuration ? velocity * (1000 / frameDuration) : 0;
}
;
}),
"[project]/node_modules/motion-utils/dist/es/pipe.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Pipe
 * Compose other transformers to run linearily
 * pipe(min(20), max(40))
 * @param  {...functions} transformers
 * @return {function}
 */ __turbopack_context__.s([
    "pipe",
    ()=>pipe
]);
const combineFunctions = (a, b)=>(v)=>b(a(v));
const pipe = (...transformers)=>transformers.reduce(combineFunctions);
;
}),
"[project]/node_modules/motion-utils/dist/es/time-conversion.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Converts seconds to milliseconds
 *
 * @param seconds - Time in seconds.
 * @return milliseconds - Converted time in milliseconds.
 */ /*#__NO_SIDE_EFFECTS__*/ __turbopack_context__.s([
    "millisecondsToSeconds",
    ()=>millisecondsToSeconds,
    "secondsToMilliseconds",
    ()=>secondsToMilliseconds
]);
const secondsToMilliseconds = (seconds)=>seconds * 1000;
/*#__NO_SIDE_EFFECTS__*/ const millisecondsToSeconds = (milliseconds)=>milliseconds / 1000;
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/cubic-bezier.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cubicBezier",
    ()=>cubicBezier
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$noop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/noop.mjs [client] (ecmascript)");
;
/*
  Bezier function generator
  This has been modified from Gaëtan Renaudeau's BezierEasing
  https://github.com/gre/bezier-easing/blob/master/src/index.js
  https://github.com/gre/bezier-easing/blob/master/LICENSE
  
  I've removed the newtonRaphsonIterate algo because in benchmarking it
  wasn't noticeably faster than binarySubdivision, indeed removing it
  usually improved times, depending on the curve.
  I also removed the lookup table, as for the added bundle size and loop we're
  only cutting ~4 or so subdivision iterations. I bumped the max iterations up
  to 12 to compensate and this still tended to be faster for no perceivable
  loss in accuracy.
  Usage
    const easeOut = cubicBezier(.17,.67,.83,.67);
    const x = easeOut(0.5); // returns 0.627...
*/ // Returns x(t) given t, x1, and x2, or y(t) given t, y1, and y2.
const calcBezier = (t, a1, a2)=>(((1.0 - 3.0 * a2 + 3.0 * a1) * t + (3.0 * a2 - 6.0 * a1)) * t + 3.0 * a1) * t;
const subdivisionPrecision = 0.0000001;
const subdivisionMaxIterations = 12;
function binarySubdivide(x, lowerBound, upperBound, mX1, mX2) {
    let currentX;
    let currentT;
    let i = 0;
    do {
        currentT = lowerBound + (upperBound - lowerBound) / 2.0;
        currentX = calcBezier(currentT, mX1, mX2) - x;
        if (currentX > 0.0) {
            upperBound = currentT;
        } else {
            lowerBound = currentT;
        }
    }while (Math.abs(currentX) > subdivisionPrecision && ++i < subdivisionMaxIterations)
    return currentT;
}
function cubicBezier(mX1, mY1, mX2, mY2) {
    // If this is a linear gradient, return linear easing
    if (mX1 === mY1 && mX2 === mY2) return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$noop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["noop"];
    const getTForX = (aX)=>binarySubdivide(aX, 0, 1, mX1, mX2);
    // If animation is at start/end, return t without easing
    return (t)=>t === 0 || t === 1 ? t : calcBezier(getTForX(t), mY1, mY2);
}
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/ease.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "easeIn",
    ()=>easeIn,
    "easeInOut",
    ()=>easeInOut,
    "easeOut",
    ()=>easeOut
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/cubic-bezier.mjs [client] (ecmascript)");
;
const easeIn = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cubicBezier"])(0.42, 0, 1, 1);
const easeOut = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cubicBezier"])(0, 0, 0.58, 1);
const easeInOut = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cubicBezier"])(0.42, 0, 0.58, 1);
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/utils/is-easing-array.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "isEasingArray",
    ()=>isEasingArray
]);
const isEasingArray = (ease)=>{
    return Array.isArray(ease) && typeof ease[0] !== "number";
};
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/modifiers/mirror.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Accepts an easing function and returns a new one that outputs mirrored values for
// the second half of the animation. Turns easeIn into easeInOut.
__turbopack_context__.s([
    "mirrorEasing",
    ()=>mirrorEasing
]);
const mirrorEasing = (easing)=>(p)=>p <= 0.5 ? easing(2 * p) / 2 : (2 - easing(2 * (1 - p))) / 2;
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/modifiers/reverse.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Accepts an easing function and returns a new one that outputs reversed values.
// Turns easeIn into easeOut.
__turbopack_context__.s([
    "reverseEasing",
    ()=>reverseEasing
]);
const reverseEasing = (easing)=>(p)=>1 - easing(1 - p);
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/back.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "backIn",
    ()=>backIn,
    "backInOut",
    ()=>backInOut,
    "backOut",
    ()=>backOut
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/cubic-bezier.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$mirror$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/modifiers/mirror.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$reverse$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/modifiers/reverse.mjs [client] (ecmascript)");
;
;
;
const backOut = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cubicBezier"])(0.33, 1.53, 0.69, 0.99);
const backIn = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$reverse$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["reverseEasing"])(backOut);
const backInOut = /*@__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$mirror$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mirrorEasing"])(backIn);
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/anticipate.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "anticipate",
    ()=>anticipate
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/back.mjs [client] (ecmascript)");
;
const anticipate = (p)=>(p *= 2) < 1 ? 0.5 * (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["backIn"])(p) : 0.5 * (2 - Math.pow(2, -10 * (p - 1)));
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/circ.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "circIn",
    ()=>circIn,
    "circInOut",
    ()=>circInOut,
    "circOut",
    ()=>circOut
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$mirror$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/modifiers/mirror.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$reverse$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/modifiers/reverse.mjs [client] (ecmascript)");
;
;
const circIn = (p)=>1 - Math.sin(Math.acos(p));
const circOut = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$reverse$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["reverseEasing"])(circIn);
const circInOut = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$modifiers$2f$mirror$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["mirrorEasing"])(circIn);
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/utils/is-bezier-definition.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "isBezierDefinition",
    ()=>isBezierDefinition
]);
const isBezierDefinition = (easing)=>Array.isArray(easing) && typeof easing[0] === "number";
;
}),
"[project]/node_modules/motion-utils/dist/es/easing/utils/map.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "easingDefinitionToFunction",
    ()=>easingDefinitionToFunction
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/errors.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$noop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/noop.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$anticipate$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/anticipate.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/back.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$circ$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/circ.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/cubic-bezier.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$ease$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/ease.mjs [client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$utils$2f$is$2d$bezier$2d$definition$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/motion-utils/dist/es/easing/utils/is-bezier-definition.mjs [client] (ecmascript)");
;
;
;
;
;
;
;
;
const easingLookup = {
    linear: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$noop$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["noop"],
    easeIn: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$ease$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["easeIn"],
    easeInOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$ease$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["easeInOut"],
    easeOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$ease$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["easeOut"],
    circIn: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$circ$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["circIn"],
    circInOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$circ$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["circInOut"],
    circOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$circ$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["circOut"],
    backIn: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["backIn"],
    backInOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["backInOut"],
    backOut: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$back$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["backOut"],
    anticipate: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$anticipate$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["anticipate"]
};
const isValidEasing = (easing)=>{
    return typeof easing === "string";
};
const easingDefinitionToFunction = (definition)=>{
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$utils$2f$is$2d$bezier$2d$definition$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["isBezierDefinition"])(definition)) {
        // If cubic bezier definition, create bezier curve
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["invariant"])(definition.length === 4, `Cubic bezier arrays must contain four numerical values.`, "cubic-bezier-length");
        const [x1, y1, x2, y2] = definition;
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$easing$2f$cubic$2d$bezier$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["cubicBezier"])(x1, y1, x2, y2);
    } else if (isValidEasing(definition)) {
        // Else lookup from table
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$motion$2d$utils$2f$dist$2f$es$2f$errors$2e$mjs__$5b$client$5d$__$28$ecmascript$29$__["invariant"])(easingLookup[definition] !== undefined, `Invalid easing type '${definition}'`, "invalid-easing-type");
        return easingLookup[definition];
    }
    return definition;
};
;
}),
"[project]/node_modules/motion-utils/dist/es/progress.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/*
  Progress within given range

  Given a lower limit and an upper limit, we return the progress
  (expressed as a number 0-1) represented by the given value, and
  limit that progress to within 0-1.

  @param [number]: Lower limit
  @param [number]: Upper limit
  @param [number]: Value to find progress within given range
  @return [number]: Progress of value within range as expressed 0-1
*/ /*#__NO_SIDE_EFFECTS__*/ __turbopack_context__.s([
    "progress",
    ()=>progress
]);
const progress = (from, to, value)=>{
    const toFromDifference = to - from;
    return toFromDifference === 0 ? 1 : (value - from) / toFromDifference;
};
;
}),
"[project]/node_modules/motion-utils/dist/es/memo.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/*#__NO_SIDE_EFFECTS__*/ __turbopack_context__.s([
    "memo",
    ()=>memo
]);
function memo(callback) {
    let result;
    return ()=>{
        if (result === undefined) result = callback();
        return result;
    };
}
;
}),
"[project]/node_modules/motion-utils/dist/es/is-object.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "isObject",
    ()=>isObject
]);
function isObject(value) {
    return typeof value === "object" && value !== null;
}
;
}),
"[project]/node_modules/react-intersection-observer/dist/index.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "InView",
    ()=>InView,
    "defaultFallbackInView",
    ()=>defaultFallbackInView,
    "observe",
    ()=>observe,
    "useInView",
    ()=>useInView,
    "useOnInView",
    ()=>useOnInView
]);
// src/InView.tsx
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/react/index.js [client] (ecmascript)");
"use client";
var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value)=>key in obj ? __defProp(obj, key, {
        enumerable: true,
        configurable: true,
        writable: true,
        value
    }) : obj[key] = value;
var __publicField = (obj, key, value)=>__defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
;
// src/observe.ts
var observerMap = /* @__PURE__ */ new Map();
var RootIds = /* @__PURE__ */ new WeakMap();
var rootId = 0;
var unsupportedValue;
function defaultFallbackInView(inView) {
    unsupportedValue = inView;
}
function getRootId(root) {
    if (!root) return "0";
    if (RootIds.has(root)) return RootIds.get(root);
    rootId += 1;
    RootIds.set(root, rootId.toString());
    return RootIds.get(root);
}
function optionsToId(options) {
    return Object.keys(options).sort().filter((key)=>options[key] !== void 0).map((key)=>{
        return `${key}_${key === "root" ? getRootId(options.root) : options[key]}`;
    }).toString();
}
function createObserver(options) {
    const id = optionsToId(options);
    let instance = observerMap.get(id);
    if (!instance) {
        const elements = /* @__PURE__ */ new Map();
        let thresholds;
        const observer = new IntersectionObserver((entries)=>{
            entries.forEach((entry)=>{
                var _a2;
                const inView = entry.isIntersecting && thresholds.some((threshold)=>entry.intersectionRatio >= threshold);
                if (options.trackVisibility && typeof entry.isVisible === "undefined") {
                    entry.isVisible = inView;
                }
                (_a2 = elements.get(entry.target)) == null ? void 0 : _a2.forEach((callback)=>{
                    callback(inView, entry);
                });
            });
        }, options);
        thresholds = observer.thresholds || (Array.isArray(options.threshold) ? options.threshold : [
            options.threshold || 0
        ]);
        instance = {
            id,
            observer,
            elements
        };
        observerMap.set(id, instance);
    }
    return instance;
}
function observe(element, callback, options = {}, fallbackInView = unsupportedValue) {
    if (typeof window.IntersectionObserver === "undefined" && fallbackInView !== void 0) {
        const bounds = element.getBoundingClientRect();
        callback(fallbackInView, {
            isIntersecting: fallbackInView,
            target: element,
            intersectionRatio: typeof options.threshold === "number" ? options.threshold : 0,
            time: 0,
            boundingClientRect: bounds,
            intersectionRect: bounds,
            rootBounds: bounds
        });
        return ()=>{};
    }
    const { id, observer, elements } = createObserver(options);
    const callbacks = elements.get(element) || [];
    if (!elements.has(element)) {
        elements.set(element, callbacks);
    }
    callbacks.push(callback);
    observer.observe(element);
    return function unobserve() {
        callbacks.splice(callbacks.indexOf(callback), 1);
        if (callbacks.length === 0) {
            elements.delete(element);
            observer.unobserve(element);
        }
        if (elements.size === 0) {
            observer.disconnect();
            observerMap.delete(id);
        }
    };
}
// src/InView.tsx
function isPlainChildren(props) {
    return typeof props.children !== "function";
}
var InView = class extends __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["Component"] {
    constructor(props){
        super(props);
        __publicField(this, "node", null);
        __publicField(this, "_unobserveCb", null);
        __publicField(this, "lastInView");
        __publicField(this, "handleNode", (node)=>{
            if (this.node) {
                this.unobserve();
                if (!node && !this.props.triggerOnce && !this.props.skip) {
                    this.setState({
                        inView: !!this.props.initialInView,
                        entry: void 0
                    });
                    this.lastInView = this.props.initialInView;
                }
            }
            this.node = node ? node : null;
            this.observeNode();
        });
        __publicField(this, "handleChange", (inView, entry)=>{
            const previousInView = this.lastInView;
            this.lastInView = inView;
            if (previousInView === void 0 && !inView) {
                return;
            }
            if (inView && this.props.triggerOnce) {
                this.unobserve();
            }
            if (!isPlainChildren(this.props)) {
                this.setState({
                    inView,
                    entry
                });
            }
            if (this.props.onChange) {
                this.props.onChange(inView, entry);
            }
        });
        this.state = {
            inView: !!props.initialInView,
            entry: void 0
        };
        this.lastInView = props.initialInView;
    }
    componentDidMount() {
        this.unobserve();
        this.observeNode();
    }
    componentDidUpdate(prevProps) {
        if (prevProps.rootMargin !== this.props.rootMargin || prevProps.root !== this.props.root || prevProps.threshold !== this.props.threshold || prevProps.skip !== this.props.skip || prevProps.trackVisibility !== this.props.trackVisibility || prevProps.delay !== this.props.delay) {
            this.unobserve();
            this.observeNode();
        }
    }
    componentWillUnmount() {
        this.unobserve();
    }
    observeNode() {
        if (!this.node || this.props.skip) return;
        const { threshold, root, rootMargin, trackVisibility, delay, fallbackInView } = this.props;
        if (this.lastInView === void 0) {
            this.lastInView = this.props.initialInView;
        }
        this._unobserveCb = observe(this.node, this.handleChange, {
            threshold,
            root,
            rootMargin,
            // @ts-expect-error
            trackVisibility,
            delay
        }, fallbackInView);
    }
    unobserve() {
        if (this._unobserveCb) {
            this._unobserveCb();
            this._unobserveCb = null;
        }
    }
    render() {
        const { children } = this.props;
        if (typeof children === "function") {
            const { inView, entry } = this.state;
            return children({
                inView,
                entry,
                ref: this.handleNode
            });
        }
        const { as, triggerOnce, threshold, root, rootMargin, onChange, skip, trackVisibility, delay, initialInView, fallbackInView, ...props } = this.props;
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["createElement"](as || "div", {
            ref: this.handleNode,
            ...props
        }, children);
    }
};
;
function useInView({ threshold, delay, trackVisibility, rootMargin, root, triggerOnce, skip, initialInView, fallbackInView, onChange } = {}) {
    var _a2;
    const [ref, setRef] = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useState"](null);
    const callback = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](onChange);
    const lastInViewRef = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](initialInView);
    const [state, setState] = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useState"]({
        inView: !!initialInView,
        entry: void 0
    });
    callback.current = onChange;
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useEffect"]({
        "useInView.useEffect": ()=>{
            if (lastInViewRef.current === void 0) {
                lastInViewRef.current = initialInView;
            }
            if (skip || !ref) return;
            let unobserve;
            unobserve = observe(ref, {
                "useInView.useEffect": (inView, entry)=>{
                    const previousInView = lastInViewRef.current;
                    lastInViewRef.current = inView;
                    if (previousInView === void 0 && !inView) {
                        return;
                    }
                    setState({
                        inView,
                        entry
                    });
                    if (callback.current) callback.current(inView, entry);
                    if (entry.isIntersecting && triggerOnce && unobserve) {
                        unobserve();
                        unobserve = void 0;
                    }
                }
            }["useInView.useEffect"], {
                root,
                rootMargin,
                threshold,
                // @ts-expect-error
                trackVisibility,
                delay
            }, fallbackInView);
            return ({
                "useInView.useEffect": ()=>{
                    if (unobserve) {
                        unobserve();
                    }
                }
            })["useInView.useEffect"];
        }
    }["useInView.useEffect"], // We break the rule here, because we aren't including the actual `threshold` variable
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
        // If the threshold is an array, convert it to a string, so it won't change between renders.
        Array.isArray(threshold) ? threshold.toString() : threshold,
        ref,
        root,
        rootMargin,
        triggerOnce,
        skip,
        trackVisibility,
        fallbackInView,
        delay
    ]);
    const entryTarget = (_a2 = state.entry) == null ? void 0 : _a2.target;
    const previousEntryTarget = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](void 0);
    if (!ref && entryTarget && !triggerOnce && !skip && previousEntryTarget.current !== entryTarget) {
        previousEntryTarget.current = entryTarget;
        setState({
            inView: !!initialInView,
            entry: void 0
        });
        lastInViewRef.current = initialInView;
    }
    const result = [
        setRef,
        state.inView,
        state.entry
    ];
    result.ref = result[0];
    result.inView = result[1];
    result.entry = result[2];
    return result;
}
;
var _a, _b;
var useSyncEffect = (_b = (_a = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useInsertionEffect"]) != null ? _a : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useLayoutEffect"]) != null ? _b : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useEffect"];
var useOnInView = (onIntersectionChange, { threshold, root, rootMargin, trackVisibility, delay, triggerOnce, skip } = {})=>{
    const onIntersectionChangeRef = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](onIntersectionChange);
    const observedElementRef = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](null);
    const observerCleanupRef = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](void 0);
    const lastInViewRef = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useRef"](void 0);
    useSyncEffect({
        "useOnInView.useSyncEffect": ()=>{
            onIntersectionChangeRef.current = onIntersectionChange;
        }
    }["useOnInView.useSyncEffect"], [
        onIntersectionChange
    ]);
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$index$2e$js__$5b$client$5d$__$28$ecmascript$29$__["useCallback"]({
        "useOnInView.useCallback": (element)=>{
            const cleanupExisting = {
                "useOnInView.useCallback.cleanupExisting": ()=>{
                    if (observerCleanupRef.current) {
                        const cleanup = observerCleanupRef.current;
                        observerCleanupRef.current = void 0;
                        cleanup();
                    }
                }
            }["useOnInView.useCallback.cleanupExisting"];
            if (element === observedElementRef.current) {
                return observerCleanupRef.current;
            }
            if (!element || skip) {
                cleanupExisting();
                observedElementRef.current = null;
                lastInViewRef.current = void 0;
                return;
            }
            cleanupExisting();
            observedElementRef.current = element;
            let destroyed = false;
            const destroyObserver = observe(element, {
                "useOnInView.useCallback.destroyObserver": (inView, entry)=>{
                    const previousInView = lastInViewRef.current;
                    lastInViewRef.current = inView;
                    if (previousInView === void 0 && !inView) {
                        return;
                    }
                    onIntersectionChangeRef.current(inView, entry);
                    if (triggerOnce && inView) {
                        stopObserving();
                    }
                }
            }["useOnInView.useCallback.destroyObserver"], {
                threshold,
                root,
                rootMargin,
                trackVisibility,
                delay
            });
            function stopObserving() {
                if (destroyed) return;
                destroyed = true;
                destroyObserver();
                observedElementRef.current = null;
                observerCleanupRef.current = void 0;
                lastInViewRef.current = void 0;
            }
            observerCleanupRef.current = stopObserving;
            return observerCleanupRef.current;
        }
    }["useOnInView.useCallback"], [
        Array.isArray(threshold) ? threshold.toString() : threshold,
        root,
        rootMargin,
        trackVisibility,
        delay,
        triggerOnce,
        skip
    ]);
};
;
 //# sourceMappingURL=index.mjs.map
}),
"[project]/node_modules/next-seo/dist/index.mjs [client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AggregateRatingJsonLd",
    ()=>AggregateRatingJsonLd,
    "ArticleJsonLd",
    ()=>ArticleJsonLd,
    "BreadcrumbJsonLd",
    ()=>BreadcrumbJsonLd,
    "CarouselJsonLd",
    ()=>CarouselJsonLd,
    "ClaimReviewJsonLd",
    ()=>ClaimReviewJsonLd,
    "CourseJsonLd",
    ()=>CourseJsonLd,
    "CreativeWorkJsonLd",
    ()=>CreativeWorkJsonLd,
    "DatasetJsonLd",
    ()=>DatasetJsonLd,
    "DiscussionForumPostingJsonLd",
    ()=>DiscussionForumPostingJsonLd,
    "EmployerAggregateRatingJsonLd",
    ()=>EmployerAggregateRatingJsonLd,
    "EventJsonLd",
    ()=>EventJsonLd,
    "FAQJsonLd",
    ()=>FAQJsonLd,
    "ImageJsonLd",
    ()=>ImageJsonLd,
    "JobPostingJsonLd",
    ()=>JobPostingJsonLd,
    "JsonLdScript",
    ()=>JsonLdScript,
    "LocalBusinessJsonLd",
    ()=>LocalBusinessJsonLd,
    "MerchantReturnPolicyJsonLd",
    ()=>MerchantReturnPolicyJsonLd,
    "MovieCarouselJsonLd",
    ()=>MovieCarouselJsonLd,
    "OrganizationJsonLd",
    ()=>OrganizationJsonLd,
    "ProductJsonLd",
    ()=>ProductJsonLd,
    "ProfilePageJsonLd",
    ()=>ProfilePageJsonLd,
    "QuizJsonLd",
    ()=>QuizJsonLd,
    "RecipeJsonLd",
    ()=>RecipeJsonLd,
    "ReviewJsonLd",
    ()=>ReviewJsonLd,
    "SoftwareApplicationJsonLd",
    ()=>SoftwareApplicationJsonLd,
    "VacationRentalJsonLd",
    ()=>VacationRentalJsonLd,
    "VideoJsonLd",
    ()=>VideoJsonLd,
    "processors",
    ()=>processors_export_exports
]);
// src/core/JsonLdScript.tsx
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/react/jsx-runtime.js [client] (ecmascript)");
var __defProp = Object.defineProperty;
var __export = (target, all)=>{
    for(var name in all)__defProp(target, name, {
        get: all[name],
        enumerable: true
    });
};
// src/utils/stringify.ts
var safeJsonLdReplacer = /* @__PURE__ */ (()=>{
    return (_, value)=>{
        switch(typeof value){
            case "object":
                if (value === null) {
                    return void 0;
                }
                return value;
            // JSON.stringify will recursively call replacer.
            case "number":
            case "boolean":
            case "bigint":
            case "string":
                return value;
            // Return all primitive values as-is
            default:
                {
                    isNever(value);
                    return void 0;
                }
        }
    };
})();
function isNever(_) {}
var stringify = (data)=>{
    const jsonString = JSON.stringify(data, safeJsonLdReplacer);
    return jsonString.replace(/<\/script>/gi, "\\u003C/script>").replace(/<!--/g, "\\u003C!--").replace(/-->/g, "--\\u003E");
};
;
function JsonLdScript({ data, id, scriptKey }) {
    if (data === null || data === void 0) {
        return null;
    }
    const jsonString = stringify(data);
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])("script", {
        type: "application/ld+json",
        id: id || scriptKey,
        "data-testid": id,
        dangerouslySetInnerHTML: {
            __html: jsonString
        }
    }, scriptKey);
}
// src/utils/processors.export.ts
var processors_export_exports = {};
__export(processors_export_exports, {
    processAccommodation: ()=>processAccommodation,
    processAddress: ()=>processAddress,
    processAggregateOffer: ()=>processAggregateOffer,
    processAggregateRating: ()=>processAggregateRating,
    processAppearance: ()=>processAppearance,
    processApplicantLocationRequirements: ()=>processApplicantLocationRequirements,
    processAuthor: ()=>processAuthor,
    processBedDetails: ()=>processBedDetails,
    processBrand: ()=>processBrand,
    processBreadcrumbItem: ()=>processBreadcrumbItem,
    processBroadcastEvent: ()=>processBroadcastEvent,
    processCertification: ()=>processCertification,
    processClaim: ()=>processClaim,
    processClaimReviewRating: ()=>processClaimReviewRating,
    processClip: ()=>processClip,
    processComment: ()=>processComment,
    processContactPoint: ()=>processContactPoint,
    processCreator: ()=>processCreator,
    processDataCatalog: ()=>processDataCatalog,
    processDataDownload: ()=>processDataDownload,
    processDefinedRegion: ()=>processDefinedRegion,
    processDirector: ()=>processDirector,
    processEducationRequirements: ()=>processEducationRequirements,
    processExperienceRequirements: ()=>processExperienceRequirements,
    processFeatureList: ()=>processFeatureList,
    processFunder: ()=>processFunder,
    processGeo: ()=>processGeo,
    processHiringOrganization: ()=>processHiringOrganization,
    processIdentifier: ()=>processIdentifier,
    processImage: ()=>processImage,
    processInstruction: ()=>processInstruction,
    processInteractionStatistic: ()=>processInteractionStatistic,
    processIsPartOf: ()=>processIsPartOf,
    processItemReviewed: ()=>processItemReviewed,
    processJobLocation: ()=>processJobLocation,
    processJobPropertyValue: ()=>processJobPropertyValue,
    processLicense: ()=>processLicense,
    processLocationFeatureSpecification: ()=>processLocationFeatureSpecification,
    processLogo: ()=>processLogo,
    processMainEntityOfPage: ()=>processMainEntityOfPage,
    processMemberProgram: ()=>processMemberProgram,
    processMemberProgramTier: ()=>processMemberProgramTier,
    processMembershipPointsEarned: ()=>processMembershipPointsEarned,
    processMerchantReturnPolicy: ()=>processMerchantReturnPolicy,
    processMonetaryAmount: ()=>processMonetaryAmount,
    processNumberOfEmployees: ()=>processNumberOfEmployees,
    processNutrition: ()=>processNutrition,
    processOffer: ()=>processOffer,
    processOfferShippingDetails: ()=>processOfferShippingDetails,
    processOpeningHours: ()=>processOpeningHours,
    processOrganization: ()=>processOrganization,
    processOrganizer: ()=>processOrganizer,
    processPeopleAudience: ()=>processPeopleAudience,
    processPerformer: ()=>processPerformer,
    processPlace: ()=>processPlace,
    processPriceSpecification: ()=>processPriceSpecification,
    processProductItemList: ()=>processProductItemList,
    processProductOffer: ()=>processProductOffer,
    processProductReview: ()=>processProductReview,
    processProductVariant: ()=>processProductVariant,
    processProvider: ()=>processProvider,
    processPublisher: ()=>processPublisher,
    processQuantitativeValue: ()=>processQuantitativeValue,
    processRating: ()=>processRating,
    processReturnPolicySeasonalOverride: ()=>processReturnPolicySeasonalOverride,
    processReview: ()=>processReview,
    processSchemaType: ()=>processSchemaType,
    processScreenshot: ()=>processScreenshot,
    processSeekToAction: ()=>processSeekToAction,
    processSharedContent: ()=>processSharedContent,
    processShippingDeliveryTime: ()=>processShippingDeliveryTime,
    processSimpleMonetaryAmount: ()=>processSimpleMonetaryAmount,
    processSizeSpecification: ()=>processSizeSpecification,
    processSpatialCoverage: ()=>processSpatialCoverage,
    processThreeDModel: ()=>processThreeDModel,
    processTierBenefit: ()=>processTierBenefit,
    processTierRequirement: ()=>processTierRequirement,
    processUnitPriceSpecification: ()=>processUnitPriceSpecification,
    processVariesBy: ()=>processVariesBy,
    processVideo: ()=>processVideo,
    processWebPageElement: ()=>processWebPageElement
});
// src/utils/processors.ts
var SCHEMA_TYPES = {
    PERSON: "Person",
    ORGANIZATION: "Organization",
    IMAGE_OBJECT: "ImageObject",
    POSTAL_ADDRESS: "PostalAddress",
    CONTACT_POINT: "ContactPoint",
    QUANTITATIVE_VALUE: "QuantitativeValue",
    GEO_COORDINATES: "GeoCoordinates",
    GEO_SHAPE: "GeoShape",
    OPENING_HOURS: "OpeningHoursSpecification",
    REVIEW: "Review",
    RATING: "Rating",
    AGGREGATE_RATING: "AggregateRating",
    MERCHANT_RETURN_POLICY: "MerchantReturnPolicy",
    MERCHANT_RETURN_POLICY_SEASONAL_OVERRIDE: "MerchantReturnPolicySeasonalOverride",
    MONETARY_AMOUNT: "MonetaryAmount",
    VIDEO_OBJECT: "VideoObject",
    INTERACTION_COUNTER: "InteractionCounter",
    BRAND: "Brand",
    CREDIT_CARD: "CreditCard",
    UNIT_PRICE_SPECIFICATION: "UnitPriceSpecification",
    MEMBER_PROGRAM: "MemberProgram",
    MEMBER_PROGRAM_TIER: "MemberProgramTier",
    BED_DETAILS: "BedDetails",
    LOCATION_FEATURE: "LocationFeatureSpecification",
    ACCOMMODATION: "Accommodation",
    PLACE: "Place",
    PERFORMING_GROUP: "PerformingGroup",
    OFFER: "Offer",
    AGGREGATE_OFFER: "AggregateOffer",
    PRICE_SPECIFICATION: "PriceSpecification",
    ITEM_LIST: "ItemList",
    LIST_ITEM: "ListItem",
    PRODUCT: "Product",
    PRODUCT_GROUP: "ProductGroup",
    NUTRITION_INFORMATION: "NutritionInformation",
    HOW_TO_STEP: "HowToStep",
    HOW_TO_SECTION: "HowToSection",
    PROPERTY_VALUE: "PropertyValue",
    CREATIVE_WORK: "CreativeWork",
    DATA_DOWNLOAD: "DataDownload",
    DATA_CATALOG: "DataCatalog",
    COUNTRY: "Country",
    STATE: "State",
    EDUCATIONAL_CREDENTIAL: "EducationalOccupationalCredential",
    OCCUPATIONAL_EXPERIENCE: "OccupationalExperienceRequirements",
    COMMENT: "Comment",
    WEB_PAGE: "WebPage",
    WEB_PAGE_ELEMENT: "WebPageElement",
    CLAIM: "Claim",
    CERTIFICATION: "Certification",
    PEOPLE_AUDIENCE: "PeopleAudience",
    SIZE_SPECIFICATION: "SizeSpecification",
    THREE_D_MODEL: "3DModel",
    DEFINED_REGION: "DefinedRegion",
    SHIPPING_DELIVERY_TIME: "ShippingDeliveryTime",
    OFFER_SHIPPING_DETAILS: "OfferShippingDetails"
};
function hasType(obj) {
    return obj !== null && typeof obj === "object" && "@type" in obj;
}
function isString(value) {
    return typeof value === "string";
}
function processSchemaType(value, schemaType, stringHandler, numberHandler) {
    if (isString(value) && stringHandler) {
        return {
            "@type": schemaType,
            ...stringHandler(value)
        };
    }
    if (typeof value === "number" && numberHandler) {
        return {
            "@type": schemaType,
            ...numberHandler(value)
        };
    }
    if (hasType(value)) {
        return value;
    }
    if (typeof value === "object" && value !== null) {
        return {
            "@type": schemaType,
            ...value
        };
    }
    return {
        "@type": schemaType
    };
}
function processOrganizationFields(org) {
    if (org.logo && !isString(org.logo)) {
        org.logo = processLogo(org.logo);
    }
    if (org.address && !isString(org.address)) {
        if (Array.isArray(org.address)) {
            org.address = org.address.map((addr)=>isString(addr) ? addr : processAddress(addr));
        } else {
            org.address = processAddress(org.address);
        }
    }
    if (org.contactPoint) {
        if (Array.isArray(org.contactPoint)) {
            org.contactPoint = org.contactPoint.map(processContactPoint);
        } else {
            org.contactPoint = processContactPoint(org.contactPoint);
        }
    }
}
function processAuthor(author) {
    if (isString(author)) {
        const orgIndicators = [
            "magazine",
            "publication",
            "company",
            "corporation",
            "corp",
            "inc",
            "llc",
            "ltd",
            "limited",
            "group",
            "foundation",
            "institute",
            "association",
            "society",
            "union",
            "times",
            "news",
            "press",
            "media",
            "network",
            "agency",
            "studio"
        ];
        const lowerName = author.toLowerCase();
        const isLikelyOrg = orgIndicators.some((indicator)=>lowerName.includes(indicator));
        if (isLikelyOrg) {
            return {
                "@type": SCHEMA_TYPES.ORGANIZATION,
                name: author
            };
        }
        return {
            "@type": SCHEMA_TYPES.PERSON,
            name: author
        };
    }
    if (hasType(author)) {
        return author;
    }
    const hasOrgProperties = "logo" in author || "address" in author || "contactPoint" in author;
    if (hasOrgProperties) {
        const org = {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            ...author
        };
        processOrganizationFields(org);
        return org;
    }
    return {
        "@type": SCHEMA_TYPES.PERSON,
        ...author
    };
}
function processImage(image) {
    if (isString(image)) {
        return image;
    }
    return processSchemaType(image, SCHEMA_TYPES.IMAGE_OBJECT);
}
function processAddress(address) {
    return processSchemaType(address, SCHEMA_TYPES.POSTAL_ADDRESS, (str)=>({
            streetAddress: str
        }), void 0);
}
function processContactPoint(contactPoint) {
    return processSchemaType(contactPoint, SCHEMA_TYPES.CONTACT_POINT);
}
function processLogo(logo) {
    return processImage(logo);
}
function processNumberOfEmployees(numberOfEmployees) {
    return processSchemaType(numberOfEmployees, SCHEMA_TYPES.QUANTITATIVE_VALUE, void 0, (num)=>({
            value: num
        }));
}
function processGeo(geo) {
    return processSchemaType(geo, SCHEMA_TYPES.GEO_COORDINATES);
}
function processOpeningHours(hours) {
    return processSchemaType(hours, SCHEMA_TYPES.OPENING_HOURS);
}
function processReview(review) {
    const processed = processSchemaType(review, SCHEMA_TYPES.REVIEW);
    if (review.reviewRating) {
        processed.reviewRating = processSchemaType(review.reviewRating, SCHEMA_TYPES.RATING);
    }
    if (review.author) {
        processed.author = processAuthor(review.author);
    }
    return processed;
}
function processBreadcrumbItem(item, position) {
    return {
        "@type": SCHEMA_TYPES.LIST_ITEM,
        position,
        ...item.name && {
            name: item.name
        },
        ...item.item && {
            item: item.item
        }
    };
}
function processPlace(location) {
    return processSchemaType(location, SCHEMA_TYPES.PLACE, (str)=>({
            name: str,
            address: {
                "@type": SCHEMA_TYPES.POSTAL_ADDRESS,
                streetAddress: str
            }
        }), void 0);
}
function processPerformer(performer) {
    if (isString(performer)) {
        return {
            "@type": SCHEMA_TYPES.PERFORMING_GROUP,
            name: performer
        };
    }
    if (hasType(performer)) {
        return performer;
    }
    const hasPersonProperties = "familyName" in performer || "givenName" in performer || "additionalName" in performer;
    return hasPersonProperties ? {
        "@type": SCHEMA_TYPES.PERSON,
        ...performer
    } : {
        "@type": SCHEMA_TYPES.PERFORMING_GROUP,
        ...performer
    };
}
function processOrganizer(organizer) {
    if (isString(organizer)) {
        return {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            name: organizer
        };
    }
    if (hasType(organizer)) {
        return organizer;
    }
    const hasPersonProperties = "familyName" in organizer || "givenName" in organizer || "additionalName" in organizer;
    return hasPersonProperties ? {
        "@type": SCHEMA_TYPES.PERSON,
        ...organizer
    } : {
        "@type": SCHEMA_TYPES.ORGANIZATION,
        ...organizer
    };
}
function processOrganization(org) {
    if (isString(org)) {
        return {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            name: org
        };
    }
    const processed = processSchemaType(org, SCHEMA_TYPES.ORGANIZATION);
    processOrganizationFields(processed);
    return processed;
}
function processOffer(offer) {
    return processSchemaType(offer, SCHEMA_TYPES.OFFER);
}
function processPublisher(publisher) {
    if (isString(publisher)) {
        return {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            name: publisher
        };
    }
    if (hasType(publisher) && publisher["@type"] === SCHEMA_TYPES.ORGANIZATION) {
        const org2 = {
            ...publisher
        };
        processOrganizationFields(org2);
        return org2;
    }
    if (hasType(publisher)) {
        return publisher;
    }
    const org = {
        "@type": SCHEMA_TYPES.ORGANIZATION,
        ...publisher
    };
    processOrganizationFields(org);
    return org;
}
function processNutrition(nutrition) {
    return {
        "@type": SCHEMA_TYPES.NUTRITION_INFORMATION,
        ...nutrition
    };
}
function processAggregateRating(rating) {
    return processSchemaType(rating, SCHEMA_TYPES.AGGREGATE_RATING);
}
function processMainEntityOfPage(mainEntityOfPage) {
    if (isString(mainEntityOfPage)) {
        return mainEntityOfPage;
    }
    return processSchemaType(mainEntityOfPage, SCHEMA_TYPES.WEB_PAGE);
}
function processSimpleMonetaryAmount(amount) {
    if (!amount) return void 0;
    if (typeof amount === "number") {
        return {
            "@type": SCHEMA_TYPES.MONETARY_AMOUNT,
            value: amount,
            currency: "USD"
        };
    }
    return processSchemaType(amount, SCHEMA_TYPES.MONETARY_AMOUNT);
}
function processReturnPolicySeasonalOverride(override) {
    return processSchemaType(override, SCHEMA_TYPES.MERCHANT_RETURN_POLICY_SEASONAL_OVERRIDE);
}
function processMerchantReturnPolicy(policy) {
    if (!policy) return policy;
    const processed = processSchemaType(policy, SCHEMA_TYPES.MERCHANT_RETURN_POLICY);
    if (processed.applicableCountry && !Array.isArray(processed.applicableCountry)) {
        processed.applicableCountry = [
            processed.applicableCountry
        ];
    }
    if (processed.returnPolicyCountry && !Array.isArray(processed.returnPolicyCountry)) {
        processed.returnPolicyCountry = [
            processed.returnPolicyCountry
        ];
    }
    if (processed.returnMethod && !Array.isArray(processed.returnMethod)) {
        processed.returnMethod = [
            processed.returnMethod
        ];
    }
    if (processed.refundType && !Array.isArray(processed.refundType)) {
        processed.refundType = [
            processed.refundType
        ];
    }
    if (processed.itemCondition && !Array.isArray(processed.itemCondition)) {
        processed.itemCondition = [
            processed.itemCondition
        ];
    }
    if (processed.returnShippingFeesAmount) {
        processed.returnShippingFeesAmount = processSimpleMonetaryAmount(processed.returnShippingFeesAmount);
    }
    if (processed.customerRemorseReturnShippingFeesAmount) {
        processed.customerRemorseReturnShippingFeesAmount = processSimpleMonetaryAmount(processed.customerRemorseReturnShippingFeesAmount);
    }
    if (processed.itemDefectReturnShippingFeesAmount) {
        processed.itemDefectReturnShippingFeesAmount = processSimpleMonetaryAmount(processed.itemDefectReturnShippingFeesAmount);
    }
    if (processed.restockingFee && typeof processed.restockingFee === "object") {
        processed.restockingFee = processSimpleMonetaryAmount(processed.restockingFee);
    }
    if (processed.returnPolicySeasonalOverride) {
        if (Array.isArray(processed.returnPolicySeasonalOverride)) {
            processed.returnPolicySeasonalOverride = processed.returnPolicySeasonalOverride.map(processReturnPolicySeasonalOverride);
        } else {
            processed.returnPolicySeasonalOverride = processReturnPolicySeasonalOverride(processed.returnPolicySeasonalOverride);
        }
    }
    return processed;
}
function processTierRequirement(requirement) {
    if (!requirement) return requirement;
    if (isString(requirement)) {
        return requirement;
    }
    if (hasType(requirement)) {
        return requirement;
    }
    if ("priceCurrency" in requirement && "price" in requirement) {
        if ("billingDuration" in requirement || "billingIncrement" in requirement || "unitCode" in requirement) {
            return {
                "@type": SCHEMA_TYPES.UNIT_PRICE_SPECIFICATION,
                ...requirement
            };
        }
    }
    if ("value" in requirement && "currency" in requirement) {
        return {
            "@type": SCHEMA_TYPES.MONETARY_AMOUNT,
            ...requirement
        };
    }
    if ("name" in requirement) {
        return {
            "@type": SCHEMA_TYPES.CREDIT_CARD,
            ...requirement
        };
    }
    return requirement;
}
function processTierBenefit(benefit) {
    const normalizeBenefit = (b)=>{
        if (b.startsWith("https://schema.org/")) {
            return b;
        }
        if (b === "TierBenefitLoyaltyPoints") {
            return "https://schema.org/TierBenefitLoyaltyPoints";
        }
        if (b === "TierBenefitLoyaltyPrice") {
            return "https://schema.org/TierBenefitLoyaltyPrice";
        }
        return b;
    };
    if (Array.isArray(benefit)) {
        return benefit.map(normalizeBenefit);
    }
    return normalizeBenefit(benefit);
}
function processMembershipPointsEarned(points) {
    if (typeof points === "number") {
        return {
            "@type": SCHEMA_TYPES.QUANTITATIVE_VALUE,
            value: points
        };
    }
    return processSchemaType(points, SCHEMA_TYPES.QUANTITATIVE_VALUE);
}
function processMemberProgramTier(tier) {
    const processed = processSchemaType(tier, SCHEMA_TYPES.MEMBER_PROGRAM_TIER);
    if (processed.hasTierBenefit) {
        processed.hasTierBenefit = processTierBenefit(processed.hasTierBenefit);
    }
    if (processed.hasTierRequirement) {
        processed.hasTierRequirement = processTierRequirement(processed.hasTierRequirement);
    }
    if (processed.membershipPointsEarned !== void 0) {
        processed.membershipPointsEarned = processMembershipPointsEarned(processed.membershipPointsEarned);
    }
    return processed;
}
function processMemberProgram(program) {
    const processed = processSchemaType(program, SCHEMA_TYPES.MEMBER_PROGRAM);
    if (processed.hasTiers) {
        if (Array.isArray(processed.hasTiers)) {
            processed.hasTiers = processed.hasTiers.map(processMemberProgramTier);
        } else {
            processed.hasTiers = processMemberProgramTier(processed.hasTiers);
        }
    }
    return processed;
}
function processVideo(video) {
    return processSchemaType(video, SCHEMA_TYPES.VIDEO_OBJECT);
}
function processBroadcastEvent(broadcast) {
    if (!broadcast) return broadcast;
    if (typeof broadcast === "object" && !("@type" in broadcast)) {
        return {
            "@type": "BroadcastEvent",
            ...broadcast
        };
    }
    return broadcast;
}
function processClip(clip) {
    if (!clip) return clip;
    if (typeof clip === "object" && !("@type" in clip)) {
        return {
            "@type": "Clip",
            ...clip
        };
    }
    return clip;
}
function processSeekToAction(action) {
    if (!action) return action;
    if (typeof action === "object" && !("@type" in action)) {
        return {
            "@type": "SeekToAction",
            ...action
        };
    }
    return action;
}
function processInstruction(instruction) {
    if (isString(instruction)) {
        return instruction;
    }
    if (hasType(instruction)) {
        if (instruction["@type"] === SCHEMA_TYPES.HOW_TO_SECTION && "itemListElement" in instruction) {
            return {
                ...instruction,
                itemListElement: instruction.itemListElement.map((item)=>processInstruction(item))
            };
        }
        return instruction;
    }
    if ("itemListElement" in instruction) {
        return {
            "@type": SCHEMA_TYPES.HOW_TO_SECTION,
            ...instruction,
            itemListElement: instruction.itemListElement.map((item)=>processInstruction(item))
        };
    }
    return {
        "@type": SCHEMA_TYPES.HOW_TO_STEP,
        ...instruction
    };
}
function processDirector(director) {
    return processSchemaType(director, SCHEMA_TYPES.PERSON, (str)=>({
            name: str
        }), void 0);
}
function processCreator(creator) {
    return Array.isArray(creator) ? creator.map(processAuthor) : processAuthor(creator);
}
function processIdentifier(identifier) {
    if (isString(identifier)) {
        return identifier;
    }
    return processSchemaType(identifier, SCHEMA_TYPES.PROPERTY_VALUE);
}
function processSpatialCoverage(spatial) {
    if (isString(spatial)) {
        return spatial;
    }
    const processed = processSchemaType(spatial, SCHEMA_TYPES.PLACE);
    if (spatial.geo && typeof spatial.geo === "object" && !hasType(spatial.geo)) {
        if ("latitude" in spatial.geo && "longitude" in spatial.geo) {
            processed.geo = processSchemaType(spatial.geo, SCHEMA_TYPES.GEO_COORDINATES);
        } else if ("box" in spatial.geo || "circle" in spatial.geo || "line" in spatial.geo || "polygon" in spatial.geo) {
            processed.geo = processSchemaType(spatial.geo, SCHEMA_TYPES.GEO_SHAPE);
        }
    }
    return processed;
}
function processDataDownload(download) {
    return processSchemaType(download, SCHEMA_TYPES.DATA_DOWNLOAD);
}
function processLicense(license) {
    if (isString(license)) {
        return license;
    }
    return processSchemaType(license, SCHEMA_TYPES.CREATIVE_WORK);
}
function processDataCatalog(catalog) {
    return processSchemaType(catalog, SCHEMA_TYPES.DATA_CATALOG);
}
function processHiringOrganization(org) {
    if (isString(org)) {
        return {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            name: org
        };
    }
    const processed = processSchemaType(org, SCHEMA_TYPES.ORGANIZATION);
    if (processed.logo && !isString(processed.logo)) {
        processed.logo = processImage(processed.logo);
    }
    return processed;
}
function processJobLocation(location) {
    if (isString(location)) {
        return {
            "@type": SCHEMA_TYPES.PLACE,
            address: {
                "@type": SCHEMA_TYPES.POSTAL_ADDRESS,
                streetAddress: location
            }
        };
    }
    const processed = processSchemaType(location, SCHEMA_TYPES.PLACE);
    if (processed.address && !isString(processed.address)) {
        processed.address = processAddress(processed.address);
    }
    return processed;
}
function processMonetaryAmount(amount) {
    const processed = processSchemaType(amount, "MonetaryAmount");
    processed.value = processSchemaType(amount.value, SCHEMA_TYPES.QUANTITATIVE_VALUE);
    return processed;
}
function processRating(rating) {
    return processSchemaType(rating, SCHEMA_TYPES.RATING);
}
function processJobPropertyValue(identifier) {
    return processSchemaType(identifier, SCHEMA_TYPES.PROPERTY_VALUE, (str)=>({
            value: str
        }), void 0);
}
function processApplicantLocationRequirements(location) {
    if (hasType(location)) {
        return location;
    }
    const name = location.name;
    const statePatterns = [
        /\b[A-Z]{2}\b/,
        // Two-letter state codes
        /\bstate\b/i,
        // Contains "state"
        /,/,
        // Contains comma (often "City, State")
        /\b(AL|AK|AZ|AR|CA|CO|CT|DE|FL|GA|HI|ID|IL|IN|IA|KS|KY|LA|ME|MD|MA|MI|MN|MS|MO|MT|NE|NV|NH|NJ|NM|NY|NC|ND|OH|OK|OR|PA|RI|SC|SD|TN|TX|UT|VT|VA|WA|WV|WI|WY)\b/
    ];
    const isState = statePatterns.some((pattern)=>pattern.test(name));
    return {
        "@type": isState ? SCHEMA_TYPES.STATE : SCHEMA_TYPES.COUNTRY,
        ...location
    };
}
function processEducationRequirements(education) {
    if (isString(education)) {
        return education;
    }
    return processSchemaType(education, SCHEMA_TYPES.EDUCATIONAL_CREDENTIAL);
}
function processExperienceRequirements(experience) {
    if (isString(experience)) {
        return experience;
    }
    return processSchemaType(experience, SCHEMA_TYPES.OCCUPATIONAL_EXPERIENCE);
}
function processInteractionStatistic(statistic) {
    return processSchemaType(statistic, SCHEMA_TYPES.INTERACTION_COUNTER);
}
function processSharedContent(content) {
    if (isString(content)) {
        return {
            "@type": SCHEMA_TYPES.WEB_PAGE,
            url: content
        };
    }
    if (hasType(content)) {
        return content;
    }
    const hasVideoProperties = "uploadDate" in content && "thumbnailUrl" in content;
    const hasImageProperties = "url" in content && ("width" in content || "height" in content);
    if (hasVideoProperties) {
        return processSchemaType(content, SCHEMA_TYPES.VIDEO_OBJECT);
    }
    if (hasImageProperties) {
        return processSchemaType(content, SCHEMA_TYPES.IMAGE_OBJECT);
    }
    return processSchemaType(content, SCHEMA_TYPES.WEB_PAGE);
}
function processComment(comment) {
    const processed = processSchemaType(comment, SCHEMA_TYPES.COMMENT);
    if (comment.author) {
        processed.author = processAuthor(comment.author);
    }
    if (comment.image && !isString(comment.image)) {
        processed.image = processImage(comment.image);
    }
    if (comment.video) {
        processed.video = processVideo(comment.video);
    }
    if (comment.interactionStatistic) {
        processed.interactionStatistic = Array.isArray(comment.interactionStatistic) ? comment.interactionStatistic.map(processInteractionStatistic) : processInteractionStatistic(comment.interactionStatistic);
    }
    if (comment.sharedContent) {
        processed.sharedContent = processSharedContent(comment.sharedContent);
    }
    if (comment.comment) {
        processed.comment = comment.comment.map(processComment);
    }
    return processed;
}
function processIsPartOf(isPartOf) {
    if (isString(isPartOf)) {
        return isPartOf;
    }
    return processSchemaType(isPartOf, SCHEMA_TYPES.CREATIVE_WORK);
}
function processBrand(brand) {
    if ("@type" in brand) {
        return brand;
    }
    if ("logo" in brand || "address" in brand || "contactPoint" in brand) {
        const org = {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            ...brand
        };
        processOrganizationFields(org);
        return org;
    }
    return processSchemaType(brand, SCHEMA_TYPES.BRAND);
}
function processBedDetails(bed) {
    return processSchemaType(bed, SCHEMA_TYPES.BED_DETAILS);
}
function processLocationFeatureSpecification(feature) {
    return processSchemaType(feature, SCHEMA_TYPES.LOCATION_FEATURE);
}
function processAccommodation(accommodation) {
    const processed = processSchemaType(accommodation, SCHEMA_TYPES.ACCOMMODATION);
    if (accommodation.bed) {
        processed.bed = Array.isArray(accommodation.bed) ? accommodation.bed.map(processBedDetails) : processBedDetails(accommodation.bed);
    }
    if (accommodation.occupancy) {
        processed.occupancy = processNumberOfEmployees(accommodation.occupancy);
    }
    if (accommodation.amenityFeature) {
        processed.amenityFeature = Array.isArray(accommodation.amenityFeature) ? accommodation.amenityFeature.map(processLocationFeatureSpecification) : processLocationFeatureSpecification(accommodation.amenityFeature);
    }
    if (accommodation.floorSize) {
        processed.floorSize = processNumberOfEmployees(accommodation.floorSize);
    }
    return processed;
}
function processProvider(provider) {
    return processSchemaType(provider, SCHEMA_TYPES.ORGANIZATION, (str)=>({
            name: str
        }), void 0);
}
function processFunder(funder) {
    if (Array.isArray(funder)) {
        return funder.map(processFunderSingle);
    }
    return processFunderSingle(funder);
}
function processFunderSingle(funder) {
    if (isString(funder)) {
        return {
            "@type": SCHEMA_TYPES.ORGANIZATION,
            name: funder
        };
    }
    if (hasType(funder)) {
        return funder;
    }
    return {
        "@type": SCHEMA_TYPES.ORGANIZATION,
        ...funder
    };
}
function processScreenshot(screenshot) {
    return processImage(screenshot);
}
function processFeatureList(features) {
    return features;
}
function processClaimReviewRating(rating) {
    return processSchemaType(rating, SCHEMA_TYPES.RATING);
}
function processClaim(claim) {
    const processed = processSchemaType(claim, SCHEMA_TYPES.CLAIM);
    if (claim.author) {
        processed.author = processAuthor(claim.author);
    }
    if (claim.appearance) {
        if (Array.isArray(claim.appearance)) {
            processed.appearance = claim.appearance.map(processAppearance);
        } else {
            processed.appearance = processAppearance(claim.appearance);
        }
    }
    if (claim.firstAppearance) {
        processed.firstAppearance = processAppearance(claim.firstAppearance);
    }
    return processed;
}
function processAppearance(appearance) {
    if (isString(appearance)) {
        return appearance;
    }
    const processed = processSchemaType(appearance, SCHEMA_TYPES.CREATIVE_WORK);
    if (appearance.author) {
        processed.author = processAuthor(appearance.author);
    }
    if (appearance.publisher && !isString(appearance.publisher)) {
        processed.publisher = processPublisher(appearance.publisher);
    }
    return processed;
}
function processWebPageElement(element) {
    return processSchemaType(element, SCHEMA_TYPES.WEB_PAGE_ELEMENT);
}
function processProductOffer(offer) {
    const processed = processSchemaType(offer, SCHEMA_TYPES.OFFER);
    if (offer.seller) {
        processed.seller = processAuthor(offer.seller);
    }
    if (offer.priceSpecification) {
        if (Array.isArray(offer.priceSpecification)) {
            processed.priceSpecification = offer.priceSpecification.map(processPriceSpecification);
        } else {
            processed.priceSpecification = processPriceSpecification(offer.priceSpecification);
        }
    }
    if (offer.hasMerchantReturnPolicy) {
        if (Array.isArray(offer.hasMerchantReturnPolicy)) {
            processed.hasMerchantReturnPolicy = offer.hasMerchantReturnPolicy.map(processMerchantReturnPolicy);
        } else {
            processed.hasMerchantReturnPolicy = processMerchantReturnPolicy(offer.hasMerchantReturnPolicy);
        }
    }
    if (offer.shippingDetails) {
        if (Array.isArray(offer.shippingDetails)) {
            processed.shippingDetails = offer.shippingDetails.map(processOfferShippingDetails);
        } else {
            processed.shippingDetails = processOfferShippingDetails(offer.shippingDetails);
        }
    }
    return processed;
}
function processAggregateOffer(offer) {
    const processed = processSchemaType(offer, SCHEMA_TYPES.AGGREGATE_OFFER);
    if (offer.offers) {
        processed.offers = offer.offers.map(processProductOffer);
    }
    return processed;
}
function processPriceSpecification(spec) {
    if ("priceType" in spec || "validForMemberTier" in spec || "membershipPointsEarned" in spec || "referenceQuantity" in spec) {
        return processUnitPriceSpecification(spec);
    }
    return processSchemaType(spec, SCHEMA_TYPES.PRICE_SPECIFICATION);
}
function processUnitPriceSpecification(spec) {
    const processed = processSchemaType(spec, SCHEMA_TYPES.UNIT_PRICE_SPECIFICATION);
    if (spec.validForMemberTier) {
        if (Array.isArray(spec.validForMemberTier)) {
            processed.validForMemberTier = spec.validForMemberTier.map(processMemberProgramTier);
        } else {
            processed.validForMemberTier = processMemberProgramTier(spec.validForMemberTier);
        }
    }
    if (spec.referenceQuantity) {
        processed.referenceQuantity = processQuantitativeValue(spec.referenceQuantity);
    }
    return processed;
}
function processQuantitativeValue(value) {
    const processed = processSchemaType(value, SCHEMA_TYPES.QUANTITATIVE_VALUE);
    if (value.valueReference) {
        processed.valueReference = processQuantitativeValue(value.valueReference);
    }
    return processed;
}
function processProductItemList(list) {
    const processed = processSchemaType(list, SCHEMA_TYPES.ITEM_LIST);
    if (list.itemListElement) {
        processed.itemListElement = list.itemListElement.map((item, index)=>{
            const processedItem = processSchemaType(item, SCHEMA_TYPES.LIST_ITEM);
            if (!processedItem.position) {
                processedItem.position = index + 1;
            }
            return processedItem;
        });
    }
    return processed;
}
function processProductReview(review) {
    const processed = processSchemaType(review, SCHEMA_TYPES.REVIEW);
    if (review.reviewRating) {
        processed.reviewRating = processSchemaType(review.reviewRating, SCHEMA_TYPES.RATING);
    }
    if (review.author) {
        processed.author = processAuthor(review.author);
    }
    if (review.positiveNotes) {
        processed.positiveNotes = processProductItemList(review.positiveNotes);
    }
    if (review.negativeNotes) {
        processed.negativeNotes = processProductItemList(review.negativeNotes);
    }
    return processed;
}
function processVariesBy(variesBy) {
    const processOne = (value)=>{
        if (value.startsWith("https://schema.org/")) {
            return value;
        }
        return `https://schema.org/${value}`;
    };
    if (Array.isArray(variesBy)) {
        return variesBy.map(processOne);
    }
    return processOne(variesBy);
}
function processProductVariant(variant) {
    if ("url" in variant && Object.keys(variant).length <= 2) {
        return variant;
    }
    const product = variant;
    if ("@type" in product) {
        return product;
    }
    const processed = {
        "@type": SCHEMA_TYPES.PRODUCT,
        ...product
    };
    if (product.image) {
        processed.image = Array.isArray(product.image) ? product.image.map(processImage) : processImage(product.image);
    }
    if (product.brand) {
        if (typeof product.brand === "string") {
            processed.brand = {
                "@type": SCHEMA_TYPES.BRAND,
                name: product.brand
            };
        } else {
            processed.brand = processBrand(product.brand);
        }
    }
    if (product.offers) {
        if (Array.isArray(product.offers)) {
            processed.offers = product.offers.map((offer)=>{
                if ("lowPrice" in offer && "priceCurrency" in offer) {
                    return processAggregateOffer(offer);
                }
                return processProductOffer(offer);
            });
        } else if ("lowPrice" in product.offers && "priceCurrency" in product.offers) {
            processed.offers = processAggregateOffer(product.offers);
        } else {
            processed.offers = processProductOffer(product.offers);
        }
    }
    if (product.review) {
        processed.review = Array.isArray(product.review) ? product.review.map(processProductReview) : processProductReview(product.review);
    }
    if (product.aggregateRating) {
        processed.aggregateRating = processAggregateRating(product.aggregateRating);
    }
    if (product.manufacturer) {
        processed.manufacturer = processAuthor(product.manufacturer);
    }
    if (product.weight && typeof product.weight === "object" && !("@type" in product.weight)) {
        processed.weight = {
            "@type": "QuantitativeValue",
            ...product.weight
        };
    }
    if (product.width && typeof product.width === "object" && !("@type" in product.width)) {
        processed.width = {
            "@type": "QuantitativeValue",
            ...product.width
        };
    }
    if (product.height && typeof product.height === "object" && !("@type" in product.height)) {
        processed.height = {
            "@type": "QuantitativeValue",
            ...product.height
        };
    }
    if (product.depth && typeof product.depth === "object" && !("@type" in product.depth)) {
        processed.depth = {
            "@type": "QuantitativeValue",
            ...product.depth
        };
    }
    return processed;
}
function processCertification(cert) {
    const processed = processSchemaType(cert, SCHEMA_TYPES.CERTIFICATION);
    if (cert.issuedBy) {
        if (typeof cert.issuedBy === "object" && !("@type" in cert.issuedBy)) {
            processed.issuedBy = {
                "@type": SCHEMA_TYPES.ORGANIZATION,
                ...cert.issuedBy
            };
        } else {
            processed.issuedBy = cert.issuedBy;
        }
    }
    if (cert.certificationRating) {
        processed.certificationRating = processRating(cert.certificationRating);
    }
    return processed;
}
function processPeopleAudience(audience) {
    const processed = processSchemaType(audience, SCHEMA_TYPES.PEOPLE_AUDIENCE);
    if (audience.suggestedAge) {
        processed.suggestedAge = processQuantitativeValue(audience.suggestedAge);
    }
    return processed;
}
function processSizeSpecification(size) {
    if (typeof size === "string") {
        return size;
    }
    return processSchemaType(size, SCHEMA_TYPES.SIZE_SPECIFICATION);
}
function processThreeDModel(model) {
    const processed = processSchemaType(model, SCHEMA_TYPES.THREE_D_MODEL);
    if (model.encoding && !("@type" in model.encoding)) {
        processed.encoding = {
            "@type": "MediaObject",
            ...model.encoding
        };
    }
    return processed;
}
function processDefinedRegion(region) {
    return processSchemaType(region, SCHEMA_TYPES.DEFINED_REGION);
}
function processShippingDeliveryTime(time) {
    const processed = processSchemaType(time, SCHEMA_TYPES.SHIPPING_DELIVERY_TIME);
    if (time.handlingTime) {
        processed.handlingTime = processQuantitativeValue(time.handlingTime);
    }
    if (time.transitTime) {
        processed.transitTime = processQuantitativeValue(time.transitTime);
    }
    return processed;
}
function processOfferShippingDetails(details) {
    const processed = processSchemaType(details, SCHEMA_TYPES.OFFER_SHIPPING_DETAILS);
    if (details.shippingRate) {
        processed.shippingRate = processSimpleMonetaryAmount(details.shippingRate);
    }
    if (details.shippingDestination) {
        if (Array.isArray(details.shippingDestination)) {
            processed.shippingDestination = details.shippingDestination.map(processDefinedRegion);
        } else {
            processed.shippingDestination = processDefinedRegion(details.shippingDestination);
        }
    }
    if (details.deliveryTime) {
        processed.deliveryTime = processShippingDeliveryTime(details.deliveryTime);
    }
    return processed;
}
function processItemReviewed(itemReviewed, defaultType) {
    if (isString(itemReviewed)) {
        return {
            "@type": defaultType || "Thing",
            name: itemReviewed
        };
    }
    if (typeof itemReviewed === "object" && itemReviewed !== null && "@type" in itemReviewed) {
        return itemReviewed;
    }
    const candidate = {
        ...itemReviewed
    };
    const inferType = ()=>{
        if (defaultType) return defaultType;
        if ("brand" in candidate || "sku" in candidate) return "Product";
        if ("recipeIngredient" in candidate || "recipeInstructions" in candidate) return "Recipe";
        if ("servesCuisine" in candidate || "address" in candidate) return "LocalBusiness";
        if ("applicationCategory" in candidate || "operatingSystem" in candidate) return "SoftwareApplication";
        if ("director" in candidate || "actor" in candidate) return "Movie";
        if ("provider" in candidate) return "Course";
        return "Thing";
    };
    return {
        "@type": inferType(),
        ...candidate
    };
}
;
function ArticleJsonLd({ type = "Article", scriptId, scriptKey, headline, url, author, datePublished, dateModified, image, publisher, description, isAccessibleForFree, mainEntityOfPage }) {
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        headline,
        ...url && {
            url
        },
        ...author && {
            author: Array.isArray(author) ? author.map(processAuthor) : processAuthor(author)
        },
        ...datePublished && {
            datePublished
        },
        ...dateModified && {
            dateModified
        },
        // If dateModified is not provided but datePublished is, use datePublished
        ...!dateModified && datePublished && {
            dateModified: datePublished
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...publisher && {
            publisher: processPublisher(publisher)
        },
        ...description && {
            description
        },
        ...isAccessibleForFree !== void 0 && {
            isAccessibleForFree
        },
        ...mainEntityOfPage && {
            mainEntityOfPage: processMainEntityOfPage(mainEntityOfPage)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `article-jsonld-${type}`
    });
}
;
function ClaimReviewJsonLd({ scriptId, scriptKey, claimReviewed, reviewRating, url, author, itemReviewed }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "ClaimReview",
        claimReviewed,
        reviewRating: processClaimReviewRating(reviewRating),
        url,
        ...author && {
            author: processAuthor(author)
        },
        ...itemReviewed && {
            itemReviewed: processClaim(itemReviewed)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "claimreview-jsonld"
    });
}
;
function CreativeWorkJsonLd({ type = "CreativeWork", scriptId, scriptKey, headline, name, url, author, datePublished, dateModified, image, publisher, description, isAccessibleForFree, hasPart, mainEntityOfPage, // Additional properties for specific types
text, // For Comment
provider, // For Course
itemReviewed, // For Review
reviewRating }) {
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        // Use headline if provided, otherwise use name
        ...headline && {
            headline
        },
        ...name && !headline && {
            name
        },
        ...url && {
            url
        },
        ...author && {
            author: Array.isArray(author) ? author.map(processAuthor) : processAuthor(author)
        },
        ...datePublished && {
            datePublished
        },
        ...dateModified && {
            dateModified
        },
        // If dateModified is not provided but datePublished is, use datePublished for certain types
        ...!dateModified && datePublished && [
            "Article",
            "NewsArticle",
            "BlogPosting"
        ].includes(type) && {
            dateModified: datePublished
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...publisher && {
            publisher: processPublisher(publisher)
        },
        ...description && {
            description
        },
        ...isAccessibleForFree !== void 0 && {
            isAccessibleForFree
        },
        ...hasPart && {
            hasPart: Array.isArray(hasPart) ? hasPart.map(processWebPageElement) : processWebPageElement(hasPart)
        },
        ...mainEntityOfPage && {
            mainEntityOfPage: processMainEntityOfPage(mainEntityOfPage)
        },
        // Type-specific properties
        ...text && type === "Comment" && {
            text
        },
        ...provider && type === "Course" && {
            provider: processPublisher(provider)
        },
        ...itemReviewed && type === "Review" && {
            itemReviewed
        },
        ...reviewRating && type === "Review" && {
            reviewRating
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `creativework-jsonld-${type.toLowerCase()}`
    });
}
;
function RecipeJsonLd({ scriptId, scriptKey, name, image, description, author, datePublished, prepTime, cookTime, totalTime, recipeYield, recipeCategory, recipeCuisine, nutrition, recipeIngredient, recipeInstructions, aggregateRating, video, keywords, url }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "Recipe",
        name,
        image: Array.isArray(image) ? image.map(processImage) : processImage(image),
        ...description && {
            description
        },
        ...author && {
            author: processAuthor(author)
        },
        ...datePublished && {
            datePublished
        },
        ...prepTime && {
            prepTime
        },
        ...cookTime && {
            cookTime
        },
        ...totalTime && {
            totalTime
        },
        ...recipeYield !== void 0 && {
            recipeYield
        },
        ...recipeCategory && {
            recipeCategory
        },
        ...recipeCuisine && {
            recipeCuisine
        },
        ...nutrition && {
            nutrition: processNutrition(nutrition)
        },
        ...recipeIngredient && {
            recipeIngredient
        },
        ...recipeInstructions && {
            recipeInstructions: Array.isArray(recipeInstructions) ? recipeInstructions.map(processInstruction) : processInstruction(recipeInstructions)
        },
        ...aggregateRating && {
            aggregateRating: processAggregateRating(aggregateRating)
        },
        ...video && {
            video: processVideo(video)
        },
        ...keywords && {
            keywords
        },
        ...url && {
            url
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "recipe-jsonld"
    });
}
;
function OrganizationJsonLd(props) {
    const { type = "Organization", scriptId, scriptKey, name, url, logo, description, sameAs, address, contactPoint, telephone, email, alternateName, foundingDate, legalName, taxID, vatID, duns, leiCode, naics, globalLocationNumber, iso6523Code, numberOfEmployees } = props;
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        ...name && {
            name
        },
        ...url && {
            url
        },
        ...logo && {
            logo: processLogo(logo)
        },
        ...description && {
            description
        },
        ...sameAs && {
            sameAs: Array.isArray(sameAs) ? sameAs : [
                sameAs
            ]
        },
        ...address && {
            address: Array.isArray(address) ? address.map(processAddress) : processAddress(address)
        },
        ...contactPoint && {
            contactPoint: Array.isArray(contactPoint) ? contactPoint.map(processContactPoint) : processContactPoint(contactPoint)
        },
        ...telephone && {
            telephone
        },
        ...email && {
            email
        },
        ...alternateName && {
            alternateName
        },
        ...foundingDate && {
            foundingDate
        },
        ...legalName && {
            legalName
        },
        ...taxID && {
            taxID
        },
        ...vatID && {
            vatID
        },
        ...duns && {
            duns
        },
        ...leiCode && {
            leiCode
        },
        ...naics && {
            naics
        },
        ...globalLocationNumber && {
            globalLocationNumber
        },
        ...iso6523Code && {
            iso6523Code
        },
        ...numberOfEmployees && {
            numberOfEmployees: processNumberOfEmployees(numberOfEmployees)
        },
        ...type === "OnlineStore" && "hasMerchantReturnPolicy" in props && props.hasMerchantReturnPolicy && {
            hasMerchantReturnPolicy: Array.isArray(props.hasMerchantReturnPolicy) ? props.hasMerchantReturnPolicy.map(processMerchantReturnPolicy) : processMerchantReturnPolicy(props.hasMerchantReturnPolicy)
        },
        ...type === "OnlineStore" && "hasMemberProgram" in props && props.hasMemberProgram && {
            hasMemberProgram: Array.isArray(props.hasMemberProgram) ? props.hasMemberProgram.map(processMemberProgram) : processMemberProgram(props.hasMemberProgram)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `organization-jsonld-${type}`
    });
}
;
function processDepartment(department) {
    return {
        "@type": department.type || "LocalBusiness",
        ...department.name && {
            name: department.name
        },
        ...department.address && {
            address: Array.isArray(department.address) ? department.address.map(processAddress) : processAddress(department.address)
        },
        ...department.url && {
            url: department.url
        },
        ...department.telephone && {
            telephone: department.telephone
        },
        ...department.image && {
            image: Array.isArray(department.image) ? department.image.map(processImage) : processImage(department.image)
        },
        ...department.priceRange && {
            priceRange: department.priceRange
        },
        ...department.geo && {
            geo: processGeo(department.geo)
        },
        ...department.openingHoursSpecification && {
            openingHoursSpecification: Array.isArray(department.openingHoursSpecification) ? department.openingHoursSpecification.map(processOpeningHours) : processOpeningHours(department.openingHoursSpecification)
        },
        ...department.review && {
            review: Array.isArray(department.review) ? department.review.map(processReview) : processReview(department.review)
        },
        ...department.aggregateRating && {
            aggregateRating: processAggregateRating(department.aggregateRating)
        },
        ...department.menu && {
            menu: department.menu
        },
        ...department.servesCuisine && {
            servesCuisine: Array.isArray(department.servesCuisine) ? department.servesCuisine : [
                department.servesCuisine
            ]
        },
        ...department.sameAs && {
            sameAs: Array.isArray(department.sameAs) ? department.sameAs : [
                department.sameAs
            ]
        },
        ...department.branchOf && {
            branchOf: department.branchOf
        },
        ...department.currenciesAccepted && {
            currenciesAccepted: department.currenciesAccepted
        },
        ...department.paymentAccepted && {
            paymentAccepted: department.paymentAccepted
        },
        ...department.areaServed && {
            areaServed: Array.isArray(department.areaServed) ? department.areaServed : [
                department.areaServed
            ]
        },
        ...department.email && {
            email: department.email
        },
        ...department.faxNumber && {
            faxNumber: department.faxNumber
        },
        ...department.slogan && {
            slogan: department.slogan
        },
        ...department.description && {
            description: department.description
        },
        ...department.publicAccess !== void 0 && {
            publicAccess: department.publicAccess
        },
        ...department.smokingAllowed !== void 0 && {
            smokingAllowed: department.smokingAllowed
        },
        ...department.isAccessibleForFree !== void 0 && {
            isAccessibleForFree: department.isAccessibleForFree
        }
    };
}
function LocalBusinessJsonLd({ type = "LocalBusiness", scriptId, scriptKey, name, address, url, telephone, image, priceRange, geo, openingHoursSpecification, review, aggregateRating, department, menu, servesCuisine, sameAs, branchOf, currenciesAccepted, paymentAccepted, areaServed, email, faxNumber, slogan, description, publicAccess, smokingAllowed, isAccessibleForFree }) {
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        name,
        address: Array.isArray(address) ? address.map(processAddress) : processAddress(address),
        ...url && {
            url
        },
        ...telephone && {
            telephone
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...priceRange && {
            priceRange
        },
        ...geo && {
            geo: processGeo(geo)
        },
        ...openingHoursSpecification && {
            openingHoursSpecification: Array.isArray(openingHoursSpecification) ? openingHoursSpecification.map(processOpeningHours) : processOpeningHours(openingHoursSpecification)
        },
        ...review && {
            review: Array.isArray(review) ? review.map(processReview) : processReview(review)
        },
        ...aggregateRating && {
            aggregateRating: processAggregateRating(aggregateRating)
        },
        ...department && {
            department: Array.isArray(department) ? department.map(processDepartment) : processDepartment(department)
        },
        ...menu && {
            menu
        },
        ...servesCuisine && {
            servesCuisine: Array.isArray(servesCuisine) ? servesCuisine : [
                servesCuisine
            ]
        },
        ...sameAs && {
            sameAs: Array.isArray(sameAs) ? sameAs : [
                sameAs
            ]
        },
        ...branchOf && {
            branchOf
        },
        ...currenciesAccepted && {
            currenciesAccepted
        },
        ...paymentAccepted && {
            paymentAccepted
        },
        ...areaServed && {
            areaServed: Array.isArray(areaServed) ? areaServed : [
                areaServed
            ]
        },
        ...email && {
            email
        },
        ...faxNumber && {
            faxNumber
        },
        ...slogan && {
            slogan
        },
        ...description && {
            description
        },
        ...publicAccess !== void 0 && {
            publicAccess
        },
        ...smokingAllowed !== void 0 && {
            smokingAllowed
        },
        ...isAccessibleForFree !== void 0 && {
            isAccessibleForFree
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `localbusiness-jsonld-${Array.isArray(type) ? type.join("-") : type}`
    });
}
;
function MerchantReturnPolicyJsonLd({ scriptId, scriptKey, applicableCountry, returnPolicyCountry, returnPolicyCategory, merchantReturnDays, returnMethod, returnFees, returnShippingFeesAmount, refundType, restockingFee, returnLabelSource, itemCondition, customerRemorseReturnFees, customerRemorseReturnShippingFeesAmount, customerRemorseReturnLabelSource, itemDefectReturnFees, itemDefectReturnShippingFeesAmount, itemDefectReturnLabelSource, returnPolicySeasonalOverride, merchantReturnLink }) {
    const returnPolicy = {
        "@type": "MerchantReturnPolicy",
        // Option B: Simple link to policy
        ...merchantReturnLink && {
            merchantReturnLink
        },
        // Option A: Detailed properties
        ...applicableCountry && {
            applicableCountry: Array.isArray(applicableCountry) ? applicableCountry : [
                applicableCountry
            ]
        },
        ...returnPolicyCountry && {
            returnPolicyCountry: Array.isArray(returnPolicyCountry) ? returnPolicyCountry : [
                returnPolicyCountry
            ]
        },
        ...returnPolicyCategory && {
            returnPolicyCategory
        },
        ...merchantReturnDays !== void 0 && {
            merchantReturnDays
        },
        ...returnMethod && {
            returnMethod: Array.isArray(returnMethod) ? returnMethod : [
                returnMethod
            ]
        },
        ...returnFees && {
            returnFees
        },
        ...returnShippingFeesAmount && {
            returnShippingFeesAmount: processSimpleMonetaryAmount(returnShippingFeesAmount)
        },
        ...refundType && {
            refundType: Array.isArray(refundType) ? refundType : [
                refundType
            ]
        },
        ...restockingFee !== void 0 && {
            restockingFee: typeof restockingFee === "number" ? restockingFee : processSimpleMonetaryAmount(restockingFee)
        },
        ...returnLabelSource && {
            returnLabelSource
        },
        ...itemCondition && {
            itemCondition: Array.isArray(itemCondition) ? itemCondition : [
                itemCondition
            ]
        },
        // Customer remorse specific properties
        ...customerRemorseReturnFees && {
            customerRemorseReturnFees
        },
        ...customerRemorseReturnShippingFeesAmount && {
            customerRemorseReturnShippingFeesAmount: processSimpleMonetaryAmount(customerRemorseReturnShippingFeesAmount)
        },
        ...customerRemorseReturnLabelSource && {
            customerRemorseReturnLabelSource
        },
        // Item defect specific properties
        ...itemDefectReturnFees && {
            itemDefectReturnFees
        },
        ...itemDefectReturnShippingFeesAmount && {
            itemDefectReturnShippingFeesAmount: processSimpleMonetaryAmount(itemDefectReturnShippingFeesAmount)
        },
        ...itemDefectReturnLabelSource && {
            itemDefectReturnLabelSource
        },
        // Seasonal override
        ...returnPolicySeasonalOverride && {
            returnPolicySeasonalOverride: Array.isArray(returnPolicySeasonalOverride) ? returnPolicySeasonalOverride.map(processReturnPolicySeasonalOverride) : processReturnPolicySeasonalOverride(returnPolicySeasonalOverride)
        }
    };
    const processedPolicy = processMerchantReturnPolicy(returnPolicy);
    const data = {
        "@context": "https://schema.org",
        ...processedPolicy
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "merchant-return-policy-jsonld"
    });
}
;
function processSummaryItem(item, index) {
    if (typeof item === "string") {
        return {
            "@type": "ListItem",
            position: index + 1,
            url: item
        };
    }
    return {
        "@type": "ListItem",
        position: item.position ?? index + 1,
        url: item.url
    };
}
function processMovieItem(movie, index) {
    const processedMovie = {
        "@type": "Movie",
        name: movie.name,
        image: Array.isArray(movie.image) ? movie.image.map(processImage) : processImage(movie.image),
        ...movie.url && {
            url: movie.url
        },
        ...movie.dateCreated && {
            dateCreated: movie.dateCreated
        },
        ...movie.director && {
            director: processDirector(movie.director)
        },
        ...movie.review && {
            review: processReview(movie.review)
        },
        ...movie.aggregateRating && {
            aggregateRating: processAggregateRating(movie.aggregateRating)
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedMovie
    };
}
function MovieCarouselJsonLd(props) {
    const { scriptId, scriptKey } = props;
    const isSummaryPage = "urls" in props;
    let itemListElement;
    if (isSummaryPage) {
        itemListElement = props.urls.map((url, index)=>processSummaryItem(url, index));
    } else {
        itemListElement = props.movies.map((movie, index)=>processMovieItem(movie, index));
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "ItemList",
        itemListElement
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "movie-carousel-jsonld"
    });
}
;
function BreadcrumbJsonLd(props) {
    const { scriptId, scriptKey } = props;
    const hasMultipleTrails = "multipleTrails" in props;
    if (hasMultipleTrails) {
        const data = props.multipleTrails.map((trail)=>({
                "@context": "https://schema.org",
                "@type": "BreadcrumbList",
                itemListElement: trail.map((item, index)=>processBreadcrumbItem(item, index + 1))
            }));
        return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
            data,
            id: scriptId,
            scriptKey: scriptKey || "breadcrumb-jsonld-multiple"
        });
    } else {
        const data = {
            "@context": "https://schema.org",
            "@type": "BreadcrumbList",
            itemListElement: props.items.map((item, index)=>processBreadcrumbItem(item, index + 1))
        };
        return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
            data,
            id: scriptId,
            scriptKey: scriptKey || "breadcrumb-jsonld"
        });
    }
}
;
function processSummaryItem2(item, index) {
    if (typeof item === "string") {
        return {
            "@type": "ListItem",
            position: index + 1,
            url: item
        };
    }
    return {
        "@type": "ListItem",
        position: item.position ?? index + 1,
        url: item.url
    };
}
function processCourseItem(course, index) {
    const processedCourse = {
        "@type": "Course",
        name: course.name,
        description: course.description,
        ...course.url && {
            url: course.url
        },
        ...course.provider && {
            provider: processProvider(course.provider)
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedCourse
    };
}
function processMovieItem2(movie, index) {
    const processedMovie = {
        "@type": "Movie",
        name: movie.name,
        image: Array.isArray(movie.image) ? movie.image.map(processImage) : processImage(movie.image),
        ...movie.url && {
            url: movie.url
        },
        ...movie.dateCreated && {
            dateCreated: movie.dateCreated
        },
        ...movie.director && {
            director: processDirector(movie.director)
        },
        ...movie.review && {
            review: processReview(movie.review)
        },
        ...movie.aggregateRating && {
            aggregateRating: processAggregateRating(movie.aggregateRating)
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedMovie
    };
}
function processRecipeItem(recipe, index) {
    const processedRecipe = {
        "@type": "Recipe",
        name: recipe.name,
        image: Array.isArray(recipe.image) ? recipe.image.map(processImage) : processImage(recipe.image),
        ...recipe.description && {
            description: recipe.description
        },
        ...recipe.author && {
            author: typeof recipe.author === "string" ? {
                "@type": "Person",
                name: recipe.author
            } : recipe.author
        },
        ...recipe.datePublished && {
            datePublished: recipe.datePublished
        },
        ...recipe.prepTime && {
            prepTime: recipe.prepTime
        },
        ...recipe.cookTime && {
            cookTime: recipe.cookTime
        },
        ...recipe.totalTime && {
            totalTime: recipe.totalTime
        },
        ...recipe.recipeYield && {
            recipeYield: recipe.recipeYield
        },
        ...recipe.recipeCategory && {
            recipeCategory: recipe.recipeCategory
        },
        ...recipe.recipeCuisine && {
            recipeCuisine: recipe.recipeCuisine
        },
        ...recipe.nutrition && {
            nutrition: processNutrition(recipe.nutrition)
        },
        ...recipe.recipeIngredient && {
            recipeIngredient: recipe.recipeIngredient
        },
        ...recipe.recipeInstructions && {
            recipeInstructions: Array.isArray(recipe.recipeInstructions) ? recipe.recipeInstructions.map(processInstruction) : processInstruction(recipe.recipeInstructions)
        },
        ...recipe.aggregateRating && {
            aggregateRating: processAggregateRating(recipe.aggregateRating)
        },
        ...recipe.video && {
            video: processVideo(recipe.video)
        },
        ...recipe.keywords && {
            keywords: recipe.keywords
        },
        ...recipe.url && {
            url: recipe.url
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedRecipe
    };
}
function processRestaurantItem(restaurant, index) {
    const address = restaurant.address ? Array.isArray(restaurant.address) ? restaurant.address.map((addr)=>typeof addr === "string" ? addr : processAddress(addr)) : typeof restaurant.address === "string" ? restaurant.address : processAddress(restaurant.address) : "";
    const processedRestaurant = {
        "@type": "Restaurant",
        name: restaurant.name,
        address,
        ...restaurant.url && {
            url: restaurant.url
        },
        ...restaurant.telephone && {
            telephone: restaurant.telephone
        },
        ...restaurant.image && {
            image: Array.isArray(restaurant.image) ? restaurant.image.map(processImage) : processImage(restaurant.image)
        },
        ...restaurant.priceRange && {
            priceRange: restaurant.priceRange
        },
        ...restaurant.geo && {
            geo: processGeo(restaurant.geo)
        },
        ...restaurant.openingHoursSpecification && {
            openingHoursSpecification: Array.isArray(restaurant.openingHoursSpecification) ? restaurant.openingHoursSpecification.map(processOpeningHours) : processOpeningHours(restaurant.openingHoursSpecification)
        },
        ...restaurant.review && {
            review: Array.isArray(restaurant.review) ? restaurant.review.map(processReview) : processReview(restaurant.review)
        },
        ...restaurant.aggregateRating && {
            aggregateRating: processAggregateRating(restaurant.aggregateRating)
        },
        ...restaurant.menu && {
            menu: restaurant.menu
        },
        ...restaurant.servesCuisine && {
            servesCuisine: restaurant.servesCuisine
        },
        ...restaurant.sameAs && {
            sameAs: restaurant.sameAs
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedRestaurant
    };
}
function CarouselJsonLd(props) {
    const { scriptId, scriptKey } = props;
    let itemListElement;
    if ("urls" in props) {
        itemListElement = props.urls.map((url, index)=>processSummaryItem2(url, index));
    } else {
        switch(props.contentType){
            case "Course":
                itemListElement = props.items.map((item, index)=>processCourseItem(item, index));
                break;
            case "Movie":
                itemListElement = props.items.map((item, index)=>processMovieItem2(item, index));
                break;
            case "Recipe":
                itemListElement = props.items.map((item, index)=>processRecipeItem(item, index));
                break;
            case "Restaurant":
                itemListElement = props.items.map((item, index)=>processRestaurantItem(item, index));
                break;
            default:
                itemListElement = [];
        }
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "ItemList",
        itemListElement
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "carousel-jsonld"
    });
}
;
function processSummaryItem3(item, index) {
    if (typeof item === "string") {
        return {
            "@type": "ListItem",
            position: index + 1,
            url: item
        };
    }
    return {
        "@type": "ListItem",
        position: item.position ?? index + 1,
        url: item.url
    };
}
function processCourseItem2(course, index) {
    const processedCourse = {
        "@type": "Course",
        name: course.name,
        description: course.description,
        ...course.url && {
            url: course.url
        },
        ...course.provider && {
            provider: processProvider(course.provider)
        }
    };
    return {
        "@type": "ListItem",
        position: index + 1,
        item: processedCourse
    };
}
function CourseJsonLd(props) {
    const { scriptId, scriptKey } = props;
    if (!("type" in props) || props.type === "single") {
        const data2 = {
            "@context": "https://schema.org",
            "@type": "Course",
            name: props.name,
            description: props.description,
            ...props.url && {
                url: props.url
            },
            ...props.provider && {
                provider: processProvider(props.provider)
            }
        };
        return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
            data: data2,
            id: scriptId,
            scriptKey: scriptKey || "course-jsonld"
        });
    }
    let itemListElement;
    if ("urls" in props && props.type === "list") {
        itemListElement = props.urls.map((url, index)=>processSummaryItem3(url, index));
    } else if ("courses" in props && props.type === "list") {
        itemListElement = props.courses.map((course, index)=>processCourseItem2(course, index));
    } else {
        itemListElement = [];
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "ItemList",
        itemListElement
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "course-list-jsonld"
    });
}
;
function EventJsonLd({ scriptId, scriptKey, name, startDate, location, endDate, description, eventStatus, image, offers, performer, organizer, previousStartDate, url }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "Event",
        name,
        startDate,
        location: processPlace(location),
        ...endDate && {
            endDate
        },
        ...description && {
            description
        },
        ...eventStatus && {
            eventStatus
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...offers && {
            offers: Array.isArray(offers) ? offers.map(processOffer) : processOffer(offers)
        },
        ...performer && {
            performer: Array.isArray(performer) ? performer.map(processPerformer) : processPerformer(performer)
        },
        ...organizer && {
            organizer: processOrganizer(organizer)
        },
        ...previousStartDate && {
            previousStartDate
        },
        ...url && {
            url
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "event-jsonld"
    });
}
;
function processQuestion(input) {
    if (typeof input === "string") {
        return {
            "@type": "Question",
            name: input,
            acceptedAnswer: {
                "@type": "Answer",
                text: ""
            }
        };
    }
    if ("question" in input && "answer" in input) {
        return {
            "@type": "Question",
            name: input.question,
            acceptedAnswer: {
                "@type": "Answer",
                text: input.answer
            }
        };
    }
    if ("name" in input) {
        const acceptedAnswer = typeof input.acceptedAnswer === "string" ? {
            "@type": "Answer",
            text: input.acceptedAnswer
        } : input.acceptedAnswer;
        return {
            "@type": "Question",
            name: input.name,
            acceptedAnswer
        };
    }
    return {
        "@type": "Question",
        name: "",
        acceptedAnswer: {
            "@type": "Answer",
            text: ""
        }
    };
}
function FAQJsonLd({ questions, scriptId, scriptKey }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        mainEntity: questions.map(processQuestion)
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "faq-jsonld"
    });
}
;
function ImageJsonLd({ scriptId, scriptKey, ...props }) {
    const processImage2 = (image)=>{
        const hasRequiredMetadata = image.creator || image.creditText || image.copyrightNotice || image.license;
        if (!image.contentUrl || !hasRequiredMetadata) {
            console.warn("ImageJsonLd: contentUrl and at least one of creator, creditText, copyrightNotice, or license is required");
        }
        return {
            "@type": "ImageObject",
            contentUrl: image.contentUrl,
            ...image.creator && {
                creator: Array.isArray(image.creator) ? image.creator.map(processAuthor) : processAuthor(image.creator)
            },
            ...image.creditText && {
                creditText: image.creditText
            },
            ...image.copyrightNotice && {
                copyrightNotice: image.copyrightNotice
            },
            ...image.license && {
                license: image.license
            },
            ...image.acquireLicensePage && {
                acquireLicensePage: image.acquireLicensePage
            }
        };
    };
    const hasMultipleImages = "images" in props && Array.isArray(props.images);
    const data = hasMultipleImages ? props.images.map(processImage2) : processImage2(props);
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data: {
            "@context": "https://schema.org",
            ...Array.isArray(data) ? {} : data,
            ...Array.isArray(data) && {
                "@graph": data
            }
        },
        id: scriptId,
        scriptKey: scriptKey || "image-jsonld"
    });
}
;
function processQuestion2(input) {
    if (typeof input === "string") {
        return {
            "@type": "Question",
            eduQuestionType: "Flashcard",
            text: input,
            acceptedAnswer: {
                "@type": "Answer",
                text: input
            }
        };
    }
    if ("question" in input && "answer" in input) {
        return {
            "@type": "Question",
            eduQuestionType: "Flashcard",
            text: input.question,
            acceptedAnswer: {
                "@type": "Answer",
                text: input.answer
            }
        };
    }
    if ("text" in input) {
        const acceptedAnswer = typeof input.acceptedAnswer === "string" ? {
            "@type": "Answer",
            text: input.acceptedAnswer
        } : input.acceptedAnswer;
        return {
            "@type": "Question",
            eduQuestionType: "Flashcard",
            text: input.text,
            acceptedAnswer
        };
    }
    return {
        "@type": "Question",
        eduQuestionType: "Flashcard",
        text: "",
        acceptedAnswer: {
            "@type": "Answer",
            text: ""
        }
    };
}
function processAbout(about) {
    if (typeof about === "string") {
        return {
            "@type": "Thing",
            name: about
        };
    }
    return {
        "@type": "Thing",
        ...about
    };
}
function processEducationalAlignment(alignment) {
    return {
        "@type": "AlignmentObject",
        alignmentType: alignment.type,
        targetName: alignment.name
    };
}
function QuizJsonLd({ questions, about, educationalAlignment, scriptId, scriptKey }) {
    const data = {
        "@context": "https://schema.org/",
        "@type": "Quiz",
        ...about && {
            about: processAbout(about)
        },
        ...educationalAlignment && {
            educationalAlignment: educationalAlignment.map(processEducationalAlignment)
        },
        hasPart: questions.map(processQuestion2)
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "quiz-jsonld"
    });
}
;
function DatasetJsonLd({ scriptId, scriptKey, name, description, url, sameAs, identifier, keywords, license, isAccessibleForFree, hasPart, isPartOf, creator, funder, includedInDataCatalog, distribution, temporalCoverage, spatialCoverage, alternateName, citation, measurementTechnique, variableMeasured, version }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "Dataset",
        name,
        description,
        ...url && {
            url
        },
        ...sameAs && {
            sameAs
        },
        ...identifier && {
            identifier: Array.isArray(identifier) ? identifier.map(processIdentifier) : processIdentifier(identifier)
        },
        ...keywords && {
            keywords
        },
        ...license && {
            license: processLicense(license)
        },
        ...isAccessibleForFree !== void 0 && {
            isAccessibleForFree
        },
        ...hasPart && {
            hasPart
        },
        ...isPartOf && {
            isPartOf
        },
        ...creator && {
            creator: processCreator(creator)
        },
        ...funder && {
            funder: processFunder(funder)
        },
        ...includedInDataCatalog && {
            includedInDataCatalog: processDataCatalog(includedInDataCatalog)
        },
        ...distribution && {
            distribution: Array.isArray(distribution) ? distribution.map(processDataDownload) : processDataDownload(distribution)
        },
        ...temporalCoverage && {
            temporalCoverage
        },
        ...spatialCoverage && {
            spatialCoverage: processSpatialCoverage(spatialCoverage)
        },
        ...alternateName && {
            alternateName
        },
        ...citation && {
            citation
        },
        ...measurementTechnique && {
            measurementTechnique
        },
        ...variableMeasured && {
            variableMeasured: Array.isArray(variableMeasured) ? variableMeasured.map((v)=>typeof v === "string" ? v : processIdentifier(v)) : typeof variableMeasured === "string" ? variableMeasured : processIdentifier(variableMeasured)
        },
        ...version && {
            version
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "dataset-jsonld"
    });
}
;
function JobPostingJsonLd({ scriptId, scriptKey, title, description, datePosted, hiringOrganization, jobLocation, url, validThrough, employmentType, identifier, baseSalary, applicantLocationRequirements, jobLocationType, directApply, educationRequirements, experienceRequirements, experienceInPlaceOfEducation }) {
    const data = {
        "@context": "https://schema.org",
        "@type": "JobPosting",
        title,
        description,
        datePosted,
        hiringOrganization: processHiringOrganization(hiringOrganization),
        ...jobLocation && {
            jobLocation: Array.isArray(jobLocation) ? jobLocation.map(processJobLocation) : processJobLocation(jobLocation)
        },
        ...url && {
            url
        },
        ...validThrough && {
            validThrough
        },
        ...employmentType && {
            employmentType: Array.isArray(employmentType) ? employmentType : employmentType
        },
        ...identifier && {
            identifier: processJobPropertyValue(identifier)
        },
        ...baseSalary && {
            baseSalary: processMonetaryAmount(baseSalary)
        },
        ...applicantLocationRequirements && {
            applicantLocationRequirements: Array.isArray(applicantLocationRequirements) ? applicantLocationRequirements.map(processApplicantLocationRequirements) : processApplicantLocationRequirements(applicantLocationRequirements)
        },
        ...jobLocationType && {
            jobLocationType
        },
        ...directApply !== void 0 && {
            directApply
        },
        ...educationRequirements && {
            educationRequirements: Array.isArray(educationRequirements) ? educationRequirements.map(processEducationRequirements) : processEducationRequirements(educationRequirements)
        },
        ...experienceRequirements && {
            experienceRequirements: processExperienceRequirements(experienceRequirements)
        },
        ...experienceInPlaceOfEducation !== void 0 && {
            experienceInPlaceOfEducation
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "jobposting-jsonld"
    });
}
;
function DiscussionForumPostingJsonLd({ type = "DiscussionForumPosting", scriptId, scriptKey, headline, text, image, video, author, datePublished, dateModified, url, comment, creativeWorkStatus, interactionStatistic, isPartOf, sharedContent }) {
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        ...headline && {
            headline
        },
        ...text && {
            text
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...video && {
            video: processVideo(video)
        },
        author: Array.isArray(author) ? author.map(processAuthor) : processAuthor(author),
        datePublished,
        ...dateModified && {
            dateModified
        },
        ...url && {
            url
        },
        ...comment && {
            comment: comment.map(processComment)
        },
        ...creativeWorkStatus && {
            creativeWorkStatus
        },
        ...interactionStatistic && {
            interactionStatistic: Array.isArray(interactionStatistic) ? interactionStatistic.map(processInteractionStatistic) : processInteractionStatistic(interactionStatistic)
        },
        ...isPartOf && {
            isPartOf: processIsPartOf(isPartOf)
        },
        ...sharedContent && {
            sharedContent: processSharedContent(sharedContent)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `${type.toLowerCase()}-jsonld`
    });
}
;
function processEmployerItemReviewed(itemReviewed) {
    if (typeof itemReviewed === "string") {
        return {
            "@type": "Organization",
            name: itemReviewed
        };
    }
    const org = {
        "@type": "Organization",
        ...itemReviewed
    };
    if ("logo" in itemReviewed && itemReviewed.logo && typeof itemReviewed.logo !== "string") {
        org.logo = processLogo(itemReviewed.logo);
    }
    if ("address" in itemReviewed && itemReviewed.address) {
        if (Array.isArray(itemReviewed.address)) {
            org.address = itemReviewed.address.map((addr)=>typeof addr === "string" ? addr : processAddress(addr));
        } else if (typeof itemReviewed.address !== "string") {
            org.address = processAddress(itemReviewed.address);
        }
    }
    if ("contactPoint" in itemReviewed && itemReviewed.contactPoint) {
        if (Array.isArray(itemReviewed.contactPoint)) {
            org.contactPoint = itemReviewed.contactPoint.map(processContactPoint);
        } else {
            org.contactPoint = processContactPoint(itemReviewed.contactPoint);
        }
    }
    if ("numberOfEmployees" in itemReviewed && itemReviewed.numberOfEmployees) {
        org.numberOfEmployees = processNumberOfEmployees(itemReviewed.numberOfEmployees);
    }
    return org;
}
function EmployerAggregateRatingJsonLd({ scriptId, scriptKey, itemReviewed, ratingValue, ratingCount, reviewCount, bestRating, worstRating }) {
    if (!ratingCount && !reviewCount) {
        throw new Error("EmployerAggregateRating requires at least one of ratingCount or reviewCount");
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "EmployerAggregateRating",
        itemReviewed: processEmployerItemReviewed(itemReviewed),
        ratingValue,
        ...ratingCount && {
            ratingCount
        },
        ...reviewCount && {
            reviewCount
        },
        ...bestRating !== void 0 && {
            bestRating
        },
        ...worstRating !== void 0 && {
            worstRating
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "employer-aggregate-rating-jsonld"
    });
}
;
function VacationRentalJsonLd({ scriptId, scriptKey, containsPlace, identifier, image, latitude, longitude, name, additionalType, address, aggregateRating, brand, checkinTime, checkoutTime, description, knowsLanguage, review, geo }) {
    const processedImages = Array.isArray(image) ? image.map(processImage) : [
        processImage(image)
    ];
    const finalLatitude = geo?.latitude ?? latitude;
    const finalLongitude = geo?.longitude ?? longitude;
    const data = {
        "@context": "https://schema.org",
        "@type": "VacationRental",
        containsPlace: processAccommodation(containsPlace),
        identifier,
        image: processedImages,
        latitude: finalLatitude,
        longitude: finalLongitude,
        name,
        ...additionalType && {
            additionalType
        },
        ...address && {
            address: processAddress(address)
        },
        ...aggregateRating && {
            aggregateRating: processAggregateRating(aggregateRating)
        },
        ...brand && {
            brand: processBrand(brand)
        },
        ...checkinTime && {
            checkinTime
        },
        ...checkoutTime && {
            checkoutTime
        },
        ...description && {
            description
        },
        ...knowsLanguage && {
            knowsLanguage: Array.isArray(knowsLanguage) ? knowsLanguage : [
                knowsLanguage
            ]
        },
        ...review && {
            review: Array.isArray(review) ? review.map(processReview) : processReview(review)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "vacationrental-jsonld"
    });
}
;
function VideoJsonLd({ type = "VideoObject", scriptId, scriptKey, name, description, thumbnailUrl, uploadDate, contentUrl, embedUrl, duration, expires, interactionStatistic, regionsAllowed, ineligibleRegion, publication, hasPart, potentialAction, author, publisher }) {
    const data = {
        "@context": "https://schema.org",
        "@type": type,
        name,
        description,
        thumbnailUrl: Array.isArray(thumbnailUrl) ? thumbnailUrl.map(processImage) : processImage(thumbnailUrl),
        uploadDate,
        ...contentUrl && {
            contentUrl
        },
        ...embedUrl && {
            embedUrl
        },
        ...duration && {
            duration
        },
        ...expires && {
            expires
        },
        ...interactionStatistic && {
            interactionStatistic: Array.isArray(interactionStatistic) ? interactionStatistic.map(processInteractionStatistic) : processInteractionStatistic(interactionStatistic)
        },
        ...regionsAllowed && {
            regionsAllowed: Array.isArray(regionsAllowed) ? regionsAllowed : [
                regionsAllowed
            ]
        },
        ...ineligibleRegion && {
            ineligibleRegion: Array.isArray(ineligibleRegion) ? ineligibleRegion : [
                ineligibleRegion
            ]
        },
        ...publication && {
            publication: Array.isArray(publication) ? publication.map(processBroadcastEvent) : processBroadcastEvent(publication)
        },
        ...hasPart && {
            hasPart: Array.isArray(hasPart) ? hasPart.map(processClip) : processClip(hasPart)
        },
        ...potentialAction && {
            potentialAction: processSeekToAction(potentialAction)
        },
        ...author && {
            author: Array.isArray(author) ? author.map(processAuthor) : processAuthor(author)
        },
        ...publisher && {
            publisher: processPublisher(publisher)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `video-jsonld-${type}`
    });
}
;
function ProfilePageJsonLd({ mainEntity, dateCreated, dateModified, scriptId, scriptKey }) {
    const processedMainEntity = processAuthor(mainEntity);
    if (processedMainEntity.interactionStatistic) {
        if (Array.isArray(processedMainEntity.interactionStatistic)) {
            processedMainEntity.interactionStatistic = processedMainEntity.interactionStatistic.map(processInteractionStatistic);
        } else {
            processedMainEntity.interactionStatistic = processInteractionStatistic(processedMainEntity.interactionStatistic);
        }
    }
    if (processedMainEntity.agentInteractionStatistic) {
        processedMainEntity.agentInteractionStatistic = processInteractionStatistic(processedMainEntity.agentInteractionStatistic);
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "ProfilePage",
        mainEntity: processedMainEntity,
        ...dateCreated && {
            dateCreated
        },
        ...dateModified && {
            dateModified
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "profile-page-jsonld"
    });
}
;
function SoftwareApplicationJsonLd({ type = "SoftwareApplication", scriptId, scriptKey, name, description, url, image, applicationCategory, applicationSubCategory, applicationSuite, operatingSystem, memoryRequirements, processorRequirements, storageRequirements, availableOnDevice, downloadUrl, installUrl, countriesSupported, countriesNotSupported, permissions, softwareVersion, releaseNotes, screenshot, featureList, offers, aggregateRating, review, author, publisher, datePublished, dateModified, contentRating }) {
    let schemaType;
    if (Array.isArray(type)) {
        schemaType = type;
    } else {
        schemaType = type;
    }
    const data = {
        "@context": "https://schema.org",
        "@type": schemaType,
        // Required properties
        ...name && {
            name
        },
        // Conditionally include properties
        ...description && {
            description
        },
        ...url && {
            url
        },
        ...image && {
            image: Array.isArray(image) ? image.map(processImage) : processImage(image)
        },
        ...applicationCategory && {
            applicationCategory
        },
        ...applicationSubCategory && {
            applicationSubCategory
        },
        ...applicationSuite && {
            applicationSuite
        },
        ...operatingSystem && {
            operatingSystem
        },
        ...memoryRequirements && {
            memoryRequirements
        },
        ...processorRequirements && {
            processorRequirements
        },
        ...storageRequirements && {
            storageRequirements
        },
        ...availableOnDevice && {
            availableOnDevice
        },
        ...downloadUrl && {
            downloadUrl
        },
        ...installUrl && {
            installUrl
        },
        ...countriesSupported && {
            countriesSupported: Array.isArray(countriesSupported) ? countriesSupported : countriesSupported
        },
        ...countriesNotSupported && {
            countriesNotSupported: Array.isArray(countriesNotSupported) ? countriesNotSupported : countriesNotSupported
        },
        ...permissions && {
            permissions: processFeatureList(permissions)
        },
        ...softwareVersion && {
            softwareVersion
        },
        ...releaseNotes && {
            releaseNotes
        },
        ...screenshot && {
            screenshot: Array.isArray(screenshot) ? screenshot.map(processScreenshot) : processScreenshot(screenshot)
        },
        ...featureList && {
            featureList: processFeatureList(featureList)
        },
        ...offers && {
            offers: Array.isArray(offers) ? offers.map(processOffer) : processOffer(offers)
        },
        ...aggregateRating && {
            aggregateRating: processAggregateRating(aggregateRating)
        },
        ...review && {
            review: Array.isArray(review) ? review.map(processReview) : processReview(review)
        },
        ...author && {
            author: processAuthor(author)
        },
        ...publisher && {
            publisher: processPublisher(publisher)
        },
        ...datePublished && {
            datePublished
        },
        ...dateModified && {
            dateModified
        },
        // Apply defaults where appropriate
        ...!dateModified && datePublished && {
            dateModified: datePublished
        },
        ...contentRating && {
            contentRating
        }
    };
    const typeKey = Array.isArray(schemaType) ? schemaType.join("-").toLowerCase() : schemaType.toLowerCase();
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || `software-application-jsonld-${typeKey}`
    });
}
;
function ProductJsonLd(props) {
    const { scriptId, scriptKey, ...rest } = props;
    const isProductGroup = "type" in rest && rest.type === "ProductGroup" || "hasVariant" in rest || "variesBy" in rest || "productGroupID" in rest;
    let data;
    if (isProductGroup) {
        const groupProps = rest;
        data = {
            "@context": "https://schema.org",
            "@type": "ProductGroup",
            name: groupProps.name,
            ...groupProps.description && {
                description: groupProps.description
            },
            ...groupProps.url && {
                url: groupProps.url
            },
            ...groupProps.image && {
                image: Array.isArray(groupProps.image) ? groupProps.image.map(processImage) : processImage(groupProps.image)
            },
            ...groupProps.brand && {
                brand: typeof groupProps.brand === "string" ? {
                    "@type": "Brand",
                    name: groupProps.brand
                } : processBrand(groupProps.brand)
            },
            ...groupProps.review && {
                review: Array.isArray(groupProps.review) ? groupProps.review.map(processProductReview) : processProductReview(groupProps.review)
            },
            ...groupProps.aggregateRating && {
                aggregateRating: processAggregateRating(groupProps.aggregateRating)
            },
            ...groupProps.audience && {
                audience: groupProps.audience
            },
            productGroupID: groupProps.productGroupID,
            ...groupProps.variesBy && {
                variesBy: processVariesBy(groupProps.variesBy)
            },
            ...groupProps.hasVariant && {
                hasVariant: groupProps.hasVariant.map(processProductVariant)
            },
            ...groupProps.pattern && {
                pattern: groupProps.pattern
            },
            ...groupProps.material && {
                material: groupProps.material
            },
            ...groupProps.category && {
                category: groupProps.category
            }
        };
    } else {
        const productProps = rest;
        if (!productProps.review && !productProps.aggregateRating && !productProps.offers && !productProps.isVariantOf) {
            console.warn("ProductJsonLd: Product structured data requires at least one of: review, aggregateRating, offers, or isVariantOf");
        }
        let processedOffers;
        if (productProps.offers) {
            if (Array.isArray(productProps.offers)) {
                processedOffers = productProps.offers.map((offer)=>{
                    if ("lowPrice" in offer && "priceCurrency" in offer) {
                        return processAggregateOffer(offer);
                    }
                    return processProductOffer(offer);
                });
            } else if ("lowPrice" in productProps.offers && "priceCurrency" in productProps.offers) {
                processedOffers = processAggregateOffer(productProps.offers);
            } else {
                processedOffers = processProductOffer(productProps.offers);
            }
        }
        let processedReview;
        if (productProps.review) {
            processedReview = Array.isArray(productProps.review) ? productProps.review.map(processProductReview) : processProductReview(productProps.review);
        }
        let processedBrand;
        if (productProps.brand) {
            if (typeof productProps.brand === "string") {
                processedBrand = {
                    "@type": "Brand",
                    name: productProps.brand
                };
            } else {
                processedBrand = processBrand(productProps.brand);
            }
        }
        const processQuantitativeValue2 = (value)=>{
            if (typeof value === "string") {
                return value;
            }
            if (value && typeof value === "object" && !("@type" in value)) {
                return {
                    "@type": "QuantitativeValue",
                    ...value
                };
            }
            return value;
        };
        data = {
            "@context": "https://schema.org",
            "@type": productProps.isCar ? [
                "Product",
                "Car"
            ] : "Product",
            name: productProps.name,
            ...productProps.description && {
                description: productProps.description
            },
            ...productProps.image && {
                image: Array.isArray(productProps.image) ? productProps.image.map(processImage) : processImage(productProps.image)
            },
            ...productProps.sku && {
                sku: productProps.sku
            },
            ...productProps.mpn && {
                mpn: productProps.mpn
            },
            ...productProps.gtin && {
                gtin: productProps.gtin
            },
            ...productProps.gtin8 && {
                gtin8: productProps.gtin8
            },
            ...productProps.gtin12 && {
                gtin12: productProps.gtin12
            },
            ...productProps.gtin13 && {
                gtin13: productProps.gtin13
            },
            ...productProps.gtin14 && {
                gtin14: productProps.gtin14
            },
            ...processedBrand && {
                brand: processedBrand
            },
            ...processedReview && {
                review: processedReview
            },
            ...productProps.aggregateRating && {
                aggregateRating: processAggregateRating(productProps.aggregateRating)
            },
            ...processedOffers && {
                offers: processedOffers
            },
            ...productProps.category && {
                category: productProps.category
            },
            ...productProps.color && {
                color: productProps.color
            },
            ...productProps.material && {
                material: productProps.material
            },
            ...productProps.model && {
                model: productProps.model
            },
            ...productProps.productID && {
                productID: productProps.productID
            },
            ...productProps.url && {
                url: productProps.url
            },
            ...productProps.weight && {
                weight: processQuantitativeValue2(productProps.weight)
            },
            ...productProps.width && {
                width: processQuantitativeValue2(productProps.width)
            },
            ...productProps.height && {
                height: processQuantitativeValue2(productProps.height)
            },
            ...productProps.depth && {
                depth: processQuantitativeValue2(productProps.depth)
            },
            ...productProps.additionalProperty && {
                additionalProperty: productProps.additionalProperty
            },
            ...productProps.manufacturer && {
                manufacturer: processAuthor(productProps.manufacturer)
            },
            ...productProps.releaseDate && {
                releaseDate: productProps.releaseDate
            },
            ...productProps.productionDate && {
                productionDate: productProps.productionDate
            },
            ...productProps.purchaseDate && {
                purchaseDate: productProps.purchaseDate
            },
            ...productProps.expirationDate && {
                expirationDate: productProps.expirationDate
            },
            ...productProps.award && {
                award: productProps.award
            },
            ...productProps.size && {
                size: processSizeSpecification(productProps.size)
            },
            ...productProps.pattern && {
                pattern: productProps.pattern
            },
            ...productProps.isbn && {
                isbn: productProps.isbn
            },
            ...productProps.hasCertification && {
                hasCertification: Array.isArray(productProps.hasCertification) ? productProps.hasCertification.map(processCertification) : processCertification(productProps.hasCertification)
            },
            ...productProps.audience && {
                audience: processPeopleAudience(productProps.audience)
            },
            ...productProps.subjectOf && {
                subjectOf: processThreeDModel(productProps.subjectOf)
            },
            ...productProps.isVariantOf && {
                isVariantOf: productProps.isVariantOf
            },
            ...productProps.inProductGroupWithID && {
                inProductGroupWithID: productProps.inProductGroupWithID
            }
        };
    }
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || (isProductGroup ? "productgroup-jsonld" : "product-jsonld")
    });
}
;
function ReviewJsonLd({ scriptId, scriptKey, author, reviewRating, itemReviewed, datePublished, reviewBody, publisher, url, mainEntityOfPage }) {
    if (!author) {
        throw new Error("Review requires an author");
    }
    const ratingValue = typeof reviewRating === "object" && reviewRating ? reviewRating.ratingValue : void 0;
    if (ratingValue === void 0) {
        throw new Error("Review requires reviewRating.ratingValue");
    }
    if (!itemReviewed) {
        throw new Error("Review requires itemReviewed when used standalone");
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "Review",
        author: processAuthor(author),
        reviewRating: {
            "@type": "Rating",
            ...reviewRating
        },
        itemReviewed: processItemReviewed(itemReviewed),
        ...datePublished && {
            datePublished
        },
        ...reviewBody && {
            reviewBody
        },
        ...publisher && {
            publisher: processPublisher(publisher)
        },
        ...url && {
            url
        },
        ...mainEntityOfPage && {
            mainEntityOfPage: processMainEntityOfPage(mainEntityOfPage)
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "review-jsonld"
    });
}
;
function AggregateRatingJsonLd({ scriptId, scriptKey, itemReviewed, ratingValue, ratingCount, reviewCount, bestRating, worstRating }) {
    if (!itemReviewed) {
        throw new Error("AggregateRating requires itemReviewed when used standalone");
    }
    if (!ratingCount && !reviewCount) {
        throw new Error("AggregateRating requires at least one of ratingCount or reviewCount");
    }
    const data = {
        "@context": "https://schema.org",
        "@type": "AggregateRating",
        itemReviewed: processItemReviewed(itemReviewed),
        ratingValue,
        ...ratingCount !== void 0 && {
            ratingCount
        },
        ...reviewCount !== void 0 && {
            reviewCount
        },
        ...bestRating !== void 0 && {
            bestRating
        },
        ...worstRating !== void 0 && {
            worstRating
        }
    };
    return /* @__PURE__ */ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$react$2f$jsx$2d$runtime$2e$js__$5b$client$5d$__$28$ecmascript$29$__["jsx"])(JsonLdScript, {
        data,
        id: scriptId,
        scriptKey: scriptKey || "aggregaterating-jsonld"
    });
}
;
 //# sourceMappingURL=index.mjs.map
}),
]);

//# sourceMappingURL=node_modules_21e1238f._.js.map